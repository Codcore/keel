# Рецензія хвилі 0058-the-norm-weighs-a-contract (§9.9)

> **ДИСПЕТЧЕРОВАНО** (автор, 2026-09-07, друга машина). Вісім знахідок
> — вісім виправлень, жодної відмови; що з кожною сталось, записано
> нижче розділом «Диспетчеризація». Роботу спиняли за словом оператора,
> щоб вона переїхала на іншу машину, і переїзд додав своє: батарея, яку
> рецензент бачив зеленою, дала тут пʼять червоних, і кожну зміряно
> також на базовому коміті хвилі — жодна не наслідок цієї роботи.

Дата: 2026-09-07. Рецензент: свіжий агент, власний клон
`/tmp/rev0058-copy` проти бази `85d8d15` в окремому дереві
`/tmp/rev0058-base`, два бінарники, два `CARGO_TARGET_DIR`, ≈15 живих
бігів, десять меж написання `files`, **шість мутантів**, дві повні
батареї до кінця (судив кодом виходу cargo, не хвостом виводу).
Двадцять восьма хвиля з машинним дорученням. Дерева автора не
торкався (`git status --porcelain` порожній).

Вердикт: **прийняти з зауваженнями** — вісім записів: чотири середні,
дві середньо-легкі, дві легкі. Важких нема.

Слова рецензента про суть: «Ядро хвилі правильне і зміряне:
суперечність §2.11↔§6.8 існувала, виняток внесено в норму й у два
суди, борг двох контрактів після релізу справді сплачено, історія
чесна, батарея зелена, обіцянка 0052 не звужена».

## Що рецензент зміряв живим

| що | вивід | код |
|---|---|---|
| батарея гілки, двічі до кінця | 121 рядок `test result`, усі `ok`; `passed=217 failed=0 ignored=0` | **0** |
| `cargo fmt --check` | порожньо | 0 |
| `keel check` гілки | дві знахідки — журнал ще попереду (штатно) | 1 |
| `keel rev` | `chores-alone-may-carry-a-contract@8f652e` — збігається з тегом проби | 0 |
| **відтворення червоного**: проба гілки проти сирців БАЗИ | `panicked at weight_test.rs:416: and §2.11 does not forbid it…` | **101** |
| замір «до» (бінарник бази): chore-хвиля з контрактом | `самі chore, а вага повна (створює чи змінює контракт у files): мусить бути легкою (§2.11)` | 1 |
| замір «після» (бінарник гілки), та сама пісочниця | того рядка нема; лишається лише «повна (§6.8), а файл народився на цій гілці» | 1 |
| вузькість: дві chore БЕЗ контракту | знахідка однакова на базі й на гілці | 1 / 1 |
| крок §9.2 над гілкою, що чіпає неназваний контракт | «назви його у files трансформи — тоді хвиля повна (§6.8, §2.11)… план окремо, робота окремо» | 0 |
| те саме англійською | «plan apart and work apart; or take the change off the branch» | 0 |
| межі написання `files` (10 випадків) | `keel/contracts/fresh.md`, `one new in keel/contracts/`, `./keel/contracts/fresh.md` — виняток діє; дві chore без контракту — знахідка лишається; `withdrawn`+контракт — без змін | — |

**Мутанти:** з шести вбито два (виняток у `check.rs`, виняток у
`next.rs`). **Вижили чотири** — і всі чотири стали знахідками R-8,
R-1, R-4 і R-5.

## Знахідки

**R-1 (середня) — нове слово кроку §9.2 не тримає ніхто.** Мутант
повернув `next-step-light-contract-chores` в обох мовах до старого,
тепер неправдивого тексту («дай їй сценарій») — батарея зелена. Assert
у `weight_test.rs:424` грає випадок, де хвиля НАЗИВАЄ контракт, а там
`next` до цього слова не доходить узагалі (`light == false`); assert
хвилі 0052 — це `contains("дай їй сценарій") || contains("§2.11")`, і
його задовольняє і старий, і новий текст. Розріз
`interaction.learnability` каже «тримає ця хвиля» — сьогодні правда,
завтра мовчки ні. **Рецензент радить закрити до `keel close`.**
Пропозиція рецензента: у `weight_of_the_branch_test` пісочниця вже є —
додати assert на суть нового слова («назви його у files», «план
окремо, робота окремо»), а не на `||`.

**R-2 (середня) — «контракт у files» читають два різні правила.**
`docs::heavy` для `OneNewIn` питає `starts_with("keel/contracts")` без
скісної (`docs.rs:196`), а виняток у `check.rs`/`next.rs` — зі
скісною. Тож `one new in keel/contractsfoo/` проходить перше і не
проходить друге, і хвиля знову «і легка, і повна» — той самий глухий
кут, задля якого хвиля існує. Наслідок: арм `Heavy::Contract` у новому
`why` тепер досяжний ЛИШЕ через це розходження, тобто слово
друкується тільки тоді, коли інструмент помиляється. Пропозиція: одне
питання в одному місці — винести «чи цей рядок називає контракт» у
`docs::` і кликати з обох.

**R-3 (середня) — `tool-launcher.md` досі каже дорелізний світ.** У
секції «Межі, названі тут» лишився недоторканим уступ: «Жоден
опублікований теґ не несе теперішньої розкладки… пін сьогодні називає
коміт або гілку — доки оператор не підійме версію і не запушить теґ».
Хибне цілком: `v1.0.0` опубліковано з цієї розкладки, `keel.toml`
тримає `version = "1.0.0"`. Це той самий клас твердження, який хвиля
виправила на пʼятнадцять рядків вище; файл уже в scope, тож правка
безкоштовна. **Рецензент радить закрити до `keel close`.**

**R-4 (середня) — вибір «питати про контракт окремо» не тримає нічого,
а слово «вузький» применшує ширину винятку.** Мутант, що читає
`matches!(docs::heavy(wave), Some(Heavy::Contract))`, проходить усю
батарею: єдина поведінкова різниця — chore-хвиля з ДВОМА і більше
трансформами і контрактом, і її не грає жодна проба. Друга половина:
один контрактний рядок вимикає §2.11 для chore-хвилі **будь-якого
розміру** — текст §2.11 це дозволяє (виняток безумовний), але хвиля
зве його «вузьким», а по осі кількости трансформ він ширший за
читання через `docs::heavy`. Норма не каже вголос, що десять chore
проходять повз «привід зупинитися» через один рядок.

**R-5 (середньо-легка) — новий текст норми тримає лише українська
сторона.** Мутант вилучив із `docs/en/METHODOLOGY-V2.md` і
`METHODOLOGY.md` уступ-виняток §2.11 і позначку §4.11 — батарея
зелена, обидва рядки «методика цього бінарника» в `keel check`
лишились зеленими. Українську тримає `translated_from`
(`speak::translation_is_current`), англійську і копію — ніщо: кістяк
порівнює розділи й номери, не речення. Хвиля 0054 будувала
`norm_marks_test` саме щоб тримати позначки в ОБОХ мовах; ця хвиля
додає дві такі позначки і виняток — і проби не розширює.

**R-6 (легка) — англійське слово кроку загубило артикль:**
`step: the branch of wave…` при тому, що всі решта тринадцять
`next-step-*` в `en.ftl` кажуть `the step: …`.

**R-7 (легка) — §8.4 каже, що на темі без слага їде «злиття», а гейт
таку тему відмовляє.** Зміряно: тема `merge: wave 0001 -- something`
на гілці хвилі → `"merge" — не red: і не трансформа хвилі…` exit 1.
Merge їде поза судом з іншої причини — він стоїть на стовбурі. Два
інші приклади того ж речення (запис рецензії, правка журналу) —
правда. Дрібніше: код відмовляє будь-якому невідомому слагу, не лише
«чужої хвилі», тож формулювання норми вужче за машину.

**R-8 (середньо-легка) — `line.name()` у винятку не тримає ніхто.**
Мутант `name()` → `as_written()` проходить батарею; живий наслідок —
`./keel/contracts/fresh.md` повертається в глухий кут, тоді як
`docs::heavy` ту саму хвилю зве повною. Це дослівно урок хвилі 0057,
записаний у коментарі `docs.rs`, застосований правильно і не покритий
пробою.

## Що рецензент перевірив і вважає триманим

Вузькість по осі, яку хвиля називає (дві chore без контракту —
знахідка на обох бінарниках, тримають дві проби); `one new in
keel/contracts/` і `./keel/contracts/x.md` під винятком — правильно;
`withdrawn`+контракт — §2.11-знахідки не мало ніколи; контракт у
не-chore трансформі — діри нема за побудовою. **Обіцянка 0052
тримається** — зміряно живцем обидва боки, текст сценарію 0052 правки
не потребує. `translated_from: c72405` записує чинну редакцію;
копія в корені лишилась копією. Новий уступ `tool-release.md` —
правда по пунктах (Cargo.lock несе версію; суд піна exit 2;
`release.sh` відмовляє на чужому теґу; слово вікна launcher справді
друкує; власний CI збирає з дерева). Історія чесна: пʼять комітів,
один `red:` справді падав (exit 101 проти сирців бази), обіцянок
заднім числом не змінювали, дрейф названо.

## Чотири питання

1. **Про що змовчали?** Про ширину винятку по осі кількости
   трансформ (R-4); про розходження двох читань «контракту» (R-2); про
   дорелізну межу в `tool-launcher.md` (R-3).
2. **Чи всі сценарії враховані?** Основні так; не враховано рівно
   один — розходження читань (`keel/contractsfoo/`).
3. **Чи реалізовано обіцяне без тихого звуження?** Реалізовано;
   звуження обіцянки 0052 нема (перевірено бігом). Єдине звуження —
   **не в обіцянці, а в тримці**: три речі, що їх мапа зве «тримає» чи
   «названо», батарея не тримає (R-1, R-4, R-5).
4. **Чи покриває проба весь сценарій?** Не весь: другу половину
   («крок каже саме це») проба перевіряє в точці, де слово кроку вже
   не звучить (R-1).

## Власні промахи рецензента (його словами)

- Перша гіпотеза про дірку (`one new in keel/contracts` без скісної)
  виявилась хибною — читач відмовляє раніше; справжнє розходження
  знайшов другим заходом.
- Перший мутант прогнав без `--no-fail-fast`, тож не знає, чи ловить
  його й друга проба.
- `keel close` не ганяв — вважав це роботою автора.
- Симетричний бік заміру піна перевірив читанням коду, а не бігом.

---

## Диспетчеризація (автор, 2026-09-07)

Вісім знахідок, вісім виправлень — жодної відмови. Рецензент просив
закрити до `keel close` дві (R-1, R-3); решта виявились дешевшими за
рядок черги, і кожна тепер має пробу або текст, який її тримає.

**R-1 — закрито пробою.** `weight_of_the_branch_test`: замість
`contains("дай їй сценарій") || contains("§2.11")` — три асерти по
суті нового слова: «назви його у files», «§6.8», «§2.11», «план
окремо, робота окремо», і окремо — що старого тексту там уже нема.
Англійську сторону тримає нова пісочниця в `weight_test`
(`chore-contract-en`, `lang = "en"`): «name it among the files»,
«plan apart and work apart», і жодного «give it a scenario».
Контрфакт зіграно: старий текст, повернений в обидва `*.ftl`, тепер
червонить обидві проби.

**R-2 — закрито зведенням двох читань в одне.**
`docs::names_a_contract(line)` — одне питання в одному місці, кличуть
`docs::heavy`, `check.rs` і `next.rs`. Читання: `keel/contracts/` зі
скісною або сама тека `keel/contracts` — і нічого більше, тож
`keel/contractsfoo/` не контракт ані для кого. Проба
(`chore-contract-lookalike`) грає саме той глухий кут, який рецензент
знайшов: хвиля з `one new in keel/contractsfoo/` тепер просто легка й
законна.

**R-3 — закрито текстом.** Уступ `tool-launcher.md` про «жоден
опублікований теґ не несе теперішньої розкладки» переписано на світ,
який стоїть: теперішню розкладку несе `v1.0.0` і жоден із старіших
(крейт v1 жив поза `tool/`); від релізу пін-версія має свою дорогу —
архіви з `.sha256` і `sh install.sh 1.0.0` без git і cargo;
`keel.toml` самого keel тримає `version = "1.0.0"`; пін ref лишається
дорогою того, хто працює над самим keel.

**R-4 — закрито пробою і словом норми.** Проба
(`chore-two-and-a-contract`): дві chore-трансформи і контракт —
законна і повна; мутант, що читає `docs::heavy` замість окремого
питання, тепер червоніє. Ширина винятку сказана вголос у §2.11 в
обох мовах і в копії: «виняток безумовний: один контрактний рядок у
`files` робить повною хвилю з будь-якою кількістю chore, тож «привід
зупинитися» лишається там, де контракту нема».

**R-5 — закрито пробою.** `norm_marks_test` тепер читає §2.11, §4.11
і §10.5 у ТРЬОХ текстах — `docs/uk`, `docs/en` і копія
`METHODOLOGY.md` — і питає суть, а не номер: виняток, його параграф,
його ціну, його безумовність і позначку текстового правила з §9.9 і
§7.10. Параграф читається одним рядком (`flat`), бо норма верстана
руками і речення живе через перенос.

**R-6 — закрито.** `the step:` в `next-step-light-contract-chores`
(en) — як у решти тринадцяти кроків.

**R-7 — закрито текстом норми.** §8.4 у трьох текстах більше не
називає злиття прикладом теми без слага: тема merge-коміту (`merge:
…`) несе слаг, і на гілці хвилі гейт її відмовляє — merge проходить
тому, що стоїть на стовбурі, де гілка не є хвилею. І відмова тепер
названа ширше, як вона є в коді: невідомий слаг, а слаг чужої хвилі
— один випадок того самого.

**R-8 — закрито пробою.** `chore-contract-dot`: `./keel/contracts/
fresh.md` у files — виняток діє. Мутант `name()` → `as_written()`
тепер червоніє.

## Батарея на цій машині (автор, 2026-09-07)

Рецензент судив батарею на своїй машині: 217 зелених, `cargo` exit 0.
Тут, у власному клоні на macOS, той самий комміт `4146b9a` дав **пʼять
червоних** — і саме тому вони зміряні спершу на БАЗІ, окремим клоном і
окремим `CARGO_TARGET_DIR`: жодна не наслідок диспетчеризації.

| проба | чому червона | що зроблено |
|---|---|---|
| `courts_mutants_test` | «PATH без cargo» питав, чи ТЕКА зветься «cargo»/«rustup»; homebrew кладе cargo в `/opt/homebrew/bin`, і install.sh пішов будувати з сирців замість відмовити | питається вміст теки |
| `launcher_fetch_test` | те саме; сторож самої проби це й сказав уголос («the bare PATH must not carry cargo») | те саме |
| `git_hand_test` | git відповідає розвʼязаним шляхом (`/private/var/…`), пісочниця зветься `/var/…` — два написання одної теки | обидва боки розвʼязуються |
| `javascript_border_test` | проба вимагала TAP-написання від ПОРАДИ; типовий звіт node — spec від node 22 | питає вибір (один тест, і саме той), не форму звіту |
| `runner_selection_test` | без UTF-8 локалі elixir губить тест «ünïcode holds» | не код: вимога до стенду, записана в `BACKLOG` |

Батарея після цього: **217 зелених, `cargo` exit 0** (node 24,
`LANG=en_US.UTF-8`). Дві межі стенду — UTF-8 локаль і node ≥ 22 —
лежать у `BACKLOG` рядками, бо вони про машину, а не про інструмент.

## Що ця диспетчеризація лишила по собі

Одне, і воно назване в `BACKLOG`: після зведення двох читань контракту
в одне (R-2) арм `Heavy::Contract` під `chores_only` став недосяжним в
обох судах, а з ним і слово `check-chores-heavy-contract`. Мертвий арм
нікому не шкодить і нічого не друкує; прибирати його разом зі словом —
робота на окрему хвилю, і рядок черги про це стоїть.
