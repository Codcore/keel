# Рецензія хвилі 0067 «a name survives its runner» (§9.9)

Два кола свіжого ока, обидва **ПОВЕРНУТИ**, і обидва по ділу.

## Коло перше: важіль був не той

Картка називала `-kernel standard_io_encoding unicode`. Автор послав
`+pc unicode` — і це не той важіль: `+pc` керує
`io:printable_range()`, а кодування потоку лишає латинським. Рецензент
зміряв байт у байт:

```
знята локаль, нічого               * test \x{456}\x{43C}…
знята локаль, +pc unicode          * test \x{456}\x{43C}…   (без змін)
знята локаль, standard_io_encoding  * test імʼя живе
```

Разом із цим впало ще дванадцять: коментар твердив «OTP 29 друкує
UTF-8 попри локаль» (неправда, зміряно), відмова била по здорових
деревах, а проби інших доріг **не могли впасти** — вони судили зелений
вивід, який імен не друкує зовсім.

## Коло друге: механізм правильний, документ розійшовся з кодом

Рецензент підтвердив власним заміром: важіль лікує (OTP 28 і 29),
проби всіх пʼяти доріг червоніють мутантом по кожній руці окремо, і
зняття ширшої відмови за §9.8 обґрунтоване. Але:

**R2-1. Картка вирішила «чуже слово сильніше», код робив навпаки.**
`co-existence` картки каже: «зламати чужий проєкт заради свого не
можна». Автор поставив наше слово ПЕРШИМ, тобто ми перебивали чуже.
**Сплачено:** наше слово йде останнім, чуже виграє — як і вирішила
картка.

**R2-2, R2-3. Контракт і шість відповідей мапи казали про відмову,
якої не було.** Повернення до рішення картки повернуло й відмову — але
**звужену до ІМЕН**, не до всього виводу: імʼя з екранами не
збігається ні з чим у файлі, а чужий тест, що друкує `\x{` у власному
рядку, суду не стосується. Контракт тепер каже обидва боки: чиє слово
виграє і що це коштує.

**R2-4. Співіснування не стерегла жодна проба** (мутант «наше слово
першим» виживав повну батарею). **Сплачено:** новий бік ставить
`ELIXIR_ERL_OPTIONS=-kernel standard_io_encoding latin1` і вимагає
відмови вголос; мутант тепер червонить.

**R2-5. Проба була вся на запереченнях** — рука, що відмовляється
судити будь-що, проходила всі твердження. **Сплачено:** кожен бік
тепер вимагає ще й коду виходу 0, тобто що суд дійшов до кінця.

**R2-8. Чуже не-UTF-8 слово зникало мовчки** (`var` замість `var_os`).
**Сплачено.**

## Відмовлено вголос

**R2-6: сім доріг обіцяно, пʼять пройдено.** minitest і Rails проба не
бере. Це справжнє звуження, і воно тут названо замість тексту коміту,
якого машина не читає. Дім — окрема хвиля разом із рештою рук.

**R2-7: поріг OTP 26 не зміряно.** `standard_io_encoding` існує з
OTP 26; зміряно на 28 і 29. Власний CI пінить 26 — першим замірником
буде він.

**R2-9:** рядок мапи обіцяв коментар «самими байтами», а коментар
несе екрани. Замір кращий за обіцяний; рядок лишається як є.

## Числа

| що | результат |
|---|---|
| батарея | **235 passed, 0 failed**; cargo exit **0** |
| `cargo fmt --check` | 0 |
| `clippy --all-targets` | 0 |
| `keel check .` | exit 0, **0 знахідок** |
| мутанти | по одному на кожну з пʼяти рук — усі червонять; «наше слово першим» — червонить |

---

# Коло третє: **ПОВЕРНУТИ** — відмова червонила здорові дерева

Механізм рецензент підтвердив власним заміром (чуже слово справді
виграє; шимом на `mix` зчитано, що дитина бачить
`«…latin1 …unicode»` — наше останнє). Але відмова, повернута другим
колом, виявилась тим самим класом вади, проти якого хвиля затіяна.

## R3-1 — хибний червоний на чужому здоровому дереві

Три дерева, кожне закривалось **зелено до хвилі** і відмовляло на
вершині, під `LANG=en_US.UTF-8`, без жодного `ELIXIR_ERL_OPTIONS`:

| дерево | база | вершина |
|---|---|---|
| тест зветься `екран \x{456} у назві` | RC=0 | **RC=2** |
| тест зветься `знак ␦ у назві` | RC=0 | **RC=2** |
| теґований тест здоровий, **сусідній нетеґований** зветься `друкує \x{1F600} як текст` | RC=0 | **RC=2** |

Останній найгірший: одне чуже імʼя, якого keel навіть не теґує,
спиняло суд над усім проєктом. І два суди на одному дереві казали
різне: `keel gate` — «теги збіжні, робота проходить», `keel close` —
відмова.

**Сплачено: умова стоїть на ФАКТІ, а не на взірці.** Рука питає, чи
несе чуже `ELIXIR_ERL_OPTIONS` слово `standard_io_encoding` — тобто чи
наше програло, — **і тільки тоді** дивиться, чи прийшло імʼя екранами.
Дерево, де ніхто кодування не забирав, судиться як судилось. Проба
тримає саме цей бік (тест, НАЗВАНИЙ екраном, плюс нетеґований сусід);
мутант «відмова без факту» її червонить.

## R3-2 — відмова називала причину, якої не міряла

Проєкт без `ELIXIR_ERL_OPTIONS` чув «власне ELIXIR_ERL_OPTIONS цього
проєкту стоїть першим і виграло». **Сплачено:** тепер це правда за
побудовою — інакше відмови нема. Порада теж виправлена: локаль тут не
рятує, бо прапор бʼє й локаль (зміряно).

## R3-4 — коментар ніс обидва протилежні твердження

«OURS GOES FIRST» із другого кола лишився поруч із «OURS GOES LAST»
третього. **Сплачено наполовину, і четверте коло це спіймало:**
старий абзац знято, а шапку модуля я не зачепив — вона й далі обіцяла
«рівно як людина в терміналі», тоді як контракт тієї ж хвилі це
заперечує. Виправлено після четвертого кола; запис «сплачено» там, де
не сплачено, гірший за відсутній — звіт читає наступний.

## R3-7 — чуже не-UTF-8 слово доходило спотвореним

`-pa /café` з байтом `e9` приходив як `/caf\357\277\275`.
**Сплачено:** значення несеться `OsString`-ом, байтами.

## Відмовлено вголос

**R3-5:** мутанти «розширити умову на весь вивід» і «половина умови»
виживали повну батарею. Перший тепер червонить (нова проба); другий —
ні, і це названо: межа умови тримається одним боком, не двома.

**R3-6:** картка обіцяє сім доріг, проба ходить пʼятьма (minitest і
Rails). На цій машині ruby нема зовсім, тож і зміряти їх ніде. Дім —
окрема хвиля.

## Числа

| що | результат |
|---|---|
| батарея | **236 passed, 0 failed**, cargo exit **0** |
| `fmt`, `clippy` | 0 |
| `keel check .` | exit 0, **0 знахідок** |

---

# Коло четверте: **ПРИЙНЯТИ З ЗАУВАЖЕННЯМИ**

Рецензент відтворив усі числа третього кола і перевірив кожну сплату
власним заміром: три дерева, що червоніли, тепер зелені (RC=0);
відмова бʼє там, де мусить (проєкт зі своїм `latin1` і не-ASCII
іменем — RC=2); вада вилікувана (те саме дерево під знятою локаллю —
RC=0); чуже слово доходить **байтами** (шим на mix показав цілий байт
`e9`). **17 мутантів, 13 червонять.**

## Виправлено на місці, без нового кола

**R4-1.** Шапка `elixir.rs` і далі обіцяла «рівно як людина в
терміналі», хоч контракт це заперечує, а мій звіт твердив, що це
сплачено. Виправлено і те, і запис у звіті.

**R4-2, R4-3.** Три відповіді мапи розійшлися з кодом після звуження:
`reliability.fault-tolerance` обіцяла відмову на три причини (лишилась
одна), `safety.fail-safe` казала «нового шляху відмови нема» (є, і він
названий), `interaction.user-assistance` обіцяла пораду про локаль і
OTP (обидві не лікують випадку, що лишився). Усі три тепер кажуть те,
що робить код, разом із тим, чого код НЕ покриває.

## У чергу — відмовлено вголос, із причиною

**R4-3 (кодова половина) і R4-5.** Умова стоїть на середовищі
(«чи забрали кодування»), а мусила б стояти на **факті дерева**: імʼя
з екранами там, де тег чекав імені, якого в батареї нема. Це знімає
відразу три речі: дерево, де проєкт сам поставив **правильне** слово і
має тест, названий екраном (сьогодні хибний червоний); дерево, де
кодування забирає сам тест через `io.setopts`; і те, що «факт» нині —
підрядок (`-pa /opt/standard_io_encoding` його вдовольняє). Це хвиля, а
не латка. Не блокує: потрібна кон'юнкція, якої сьогодні ні в кого
нема, а хвиля прибирає незрівнянно більше хибних червоних, ніж лишає.

**R4-4.** Сплату «чуже слово байтами» не стереже проба — мутант `var`
замість `var_os` переживає повну батарею. Поведінка правильна і
зміряна; бракує заслону.

**R4-6.** minitest і Rails проба не проходить; на цій машині ruby нема
зовсім. Дім — окрема хвиля.

## Числа

| що | результат |
|---|---|
| батарея | **236 passed, 0 failed**, cargo exit 0 |
| `fmt`, `clippy` | 0 |
| `keel check .` | exit 0, 0 знахідок, 102 документи |
| `keel close .` | exit 0, 0067 **закрита**, батарея 236 × 3 зелена |
| дрейф §4.6 | нуль: `files:` на HEAD слово в слово як у плані |
| мапа | 40/40 пройдено рецензентом; три відповіді виправлено вище |
| §5.7 | `tool-adapter-elixir` — редакції не тримає жодна хвиля; 146 сигнатур звірено |

---

*Чотири кола, вирок четвертого — **ПРИЙНЯТИ З ЗАУВАЖЕННЯМИ**. У чергу
пішли: судити за фактом дерева замість середовища (знімає два хибні
червоні разом); проба на не-UTF-8 чуже слово; minitest і Rails.*
