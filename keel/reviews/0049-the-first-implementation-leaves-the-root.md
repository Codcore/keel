# Рецензія хвилі 0049-the-first-implementation-leaves-the-root (§9.9)

Дата: 2026-09-05. Рецензент: свіжий агент, власний `git clone
--no-local`, база `a5f0cf9`, окремий `CARGO_TARGET_DIR`, toolchain
1.94.1. Легка chore-хвиля (§6.8): одна трансформа без сценаріїв.

Вердикт: **прийняти** — чотири знахідки, **усі легкі**.

Хвиля робить рівно те, що каже: видалено код першої реалізації
(18 файлів, 14 407 рядків), жодного документа методики; ніщо в
репозиторії не кличе `keel.py`; батарея крейта до і після побайтово
та сама (173 = 173, нуль червоних); scope сходиться файл у файл; мапа
— 40 розрізів, кожен рівно раз; контрактів не торкнуто.

## Зміряно рецензентом

| що | число |
|---|---|
| батарея гілки / бази `a5f0cf9` | 173 / 173 passed, 0 failed |
| `keel close .` на гілці | 173 × 3 зелені; блокер 1 — цей файл |
| `keel check .` гілка / база | 78 / 77 документів, 0 / 0 знахідок; diff — три очікувані рядки |
| бінарник бази = бінарник гілки | sha256 той самий; `tool/` у diff-і нема |
| видалено | 18 файлів, усі `D`, без перейменувань; `keel.py` — 6 494 рядки, 312 816 байт |
| `grep keel.py` у yml/sh/toml/rs/json/ftl, hook-ах, скілах | 0 |
| теґи | 19 (`v0.2.0…v0.8.11`), усі несуть `keel.py`, жоден — `tool/` |
| `keel review .` | дрейф порожній; вплив контрактів порожній; мапа 40 |

## Знахідки

- **R-1 (легка).** `docs/uk/README.md` — README першої реалізації без
  позначки: рядок 202 велить `python3 -m unittest discover -s tests`,
  чого після хвилі нема; мапа звала його «історією» словами
  NEW-CONCEPT, не його власними; журнал V2-PROCESS згадує `keel.py`
  пʼять разів і в переписі не названий.
- **R-2 (легка).** Кореневий `.gitignore` — рівно `__pycache__/` і
  `*.pyc` — після хвилі єдиний слід Python у репозиторії без жодного
  `.py`.
- **R-3 (легка).** `reliability.faultlessness` казала «177 зелених» —
  зміряно 173 на базі і на гілці.
- **R-4 (легка, поза хвилею).** `keel close` каже «повна хвиля не
  зливається недоведеною» і про легку: рядок `close-blockers`
  фіксований, ваги не дивиться. Дефект інструмента.

## Чотири питання — відповіді рецензента

1. **Змовчали** про README v1 і про `.gitignore`; §8.6 у коміті — без
   «хто вирішив», але мандат простежується двома рядками BACKLOG.
2. **Сценарії:** chore без обіцянок — правильно; що можна виміряти —
   тотожність батареї і `check` до/після — виміряно.
3. **Тихе звуження:** нема — 18 `D`, README каже «one tool», BACKLOG
   знято два рядки.
4. **Кут:** сценаріїв нема; мірило 173 = 173, 0 = 0.

Дрейф — порожній (файл хвилі і робота в одному коміті); мапа — 40,
дублів 0; вплив контрактів — порожній.

## Диспетчеризація автора

**Усі чотири зроблено.** **R-1** — `docs/uk/README.md` дістав першим
рядком позначку «README першої реалізації (v1)» з відсиланням до
крейта і до цієї хвилі; слово мапи переписано (README v1 каже це сам;
V2-PROCESS названо історією); **R-2** — кореневий `.gitignore` знято
(крейт має свій `tool/.gitignore`, і нагадування про ігнорування
читає його); обидва файли дописано у scope після першого коміту —
розширення видиме (§4.6) і назване у файлі хвилі; **R-3** — 173;
**R-4** — рядок у BACKLOG.

Батарея після: 173 зелених; `keel check .` — 0 знахідок.

## Що лишилось поза хвилею

- слово `close-blockers` про вагу хвилі (R-4) — у черзі;
- `docs/uk/{METHODOLOGY,PRINCIPLES,QUALITY}.md` — решта набору v1 без
  позначки; README v1 тепер каже це за всіх, окремої позначки на
  кожному ця хвиля не ставить.
