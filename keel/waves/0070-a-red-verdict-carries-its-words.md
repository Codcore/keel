---
scenarios:
  a-red-gate-carries-the-words-that-made-it-red:
    covers:
      - functional.correctness
      - reliability.faultlessness
      - maintainability.testability
      - interaction.user-error-protection
      - interaction.user-assistance

transforms:
  the-gate-hands-over-its-output:
    implements: [a-red-gate-carries-the-words-that-made-it-red]
    files:
      - tool/src/close.rs
      - tool/i18n/uk.ftl
      - tool/i18n/en.ftl
      - keel/contracts/tool-close.md
      - tool/tests/red_carries_words_test.rs
decisions:
  functional.completeness: "хвиля повна тим, що бере ОДНУ половину issue #45 — вивід gate — і бере її цілком: `run_command` кличеться з двох місць, тож міняється вирок і `ci` проєкту, і `verify` контрактів. Друга половина issue (німа батарея) винесена в окрему хвилю, бо зміряно, що вона вчетверо дорожча, ніж автор спершу написав"
  functional.appropriateness: "віддаємо те, що вже маємо. `close.rs:688` бере `child.output()` — увесь stdout і stderr у пам'яті, — і лишає один останній непорожній рядок. Це не «здобути слово», це перестати його викидати"
  performance.time-behaviour: "не застосовується, бо вивід уже в пам'яті: різати вікно і друкувати рядки коштує стільки, скільки коштує сам друк"
  performance.capacity: "названо ціну числом, і воно більше, ніж здається. Зміряно рецензентом на власному `ci` keel: `cargo fmt --check` над ОДНИМ зіпсованим файлом дав 38 793 байти і 636 рядків. Десятикроковий `bin/ci` чужого проєкту — це десятки тисяч. Вікно мусить бути НА КОМАНДУ, а не на звіт, інакше десятий verify з'їдає бюджет першого; і на весь звіт потрібна стеля, бо `--json` кладе його в ОДИН рядок поля report"
  performance.resource-utilisation: "не застосовується, бо ні диска, ні процесів хвиля не заводить — вона друкує те, що вже прочитала"
  compatibility.co-existence: "названо ціну для того, хто пінує keel: звіт `keel close` стане помітно більшим, і хто читає його очима або грепом, побачить нові рядки. Зате зникає обхід, який чужий проєкт зробив у себе — окремий крок workflow, що жене роботу двічі"
  compatibility.interoperability: "названо межу: `close --json` не має структури, окрім `blockers` і `report` (`main.rs:472`, і `json.rs` сам це пише). Тому ламати нема чого — але саме тому хвиля мусить вирішити, чи слово падіння йде в ТИПІЗОВАНЕ поле, чи лише в прозу report. Друге означає, що harness мусить парсити прозу — рівно те, від чого json і робили"
  interaction.appropriateness-recognisability: "тримає: сьогодні `ВПАВ ({ $words })` читається як тривалість — у полі це дало `(Done in 116ms)`, і читач розумно вирішив, що gate ледве встиг побігти. Дужки після «ВПАВ» мусять піти"
  interaction.learnability: "тримає: рамка вікна — нова річ, яку читач мусить навчитися читати. Скільки показано, скільки зрізано, чий це вивід — без цього вікно стане новим мовчанням, тільки довшим"
  interaction.operability: "тримає: обхід, який чужий проєкт тримає в себе — окремий крок workflow перед `keel close`, — знімається, і робота перестає гнатися двічі. Це і є зміна порядку роботи, а не лише того, що видно"
  interaction.user-engagement: "тримає: вивід росте, і росте сильно — 636 рядків з ОДНОГО кроку. Казати «нічого не показує понад те, що вже показує» було б неправдою поруч із власною вимогою назвати винуватця"
  interaction.inclusivity: "названо межу: вивід чужого раннера приходить ЙОГО мовою і його кодуванням, і суд не має вдавати, що переклав. Рамка навколо цитати — наша, сама цитата — чужа, дослівна"
  interaction.self-descriptiveness: "тримає: звіт мусить показати, що саме він цитує — чий вивід, скільки рядків показано, скільки зрізано"
  reliability.fault-tolerance: "названо межу: вивід може бути порожній (уже має слово `close-verify-no-words`) або не-UTF-8. Друге — та сама родина, що elixir під порожньою локаллю: суд не має мовчки підставляти знак заміни"
  reliability.availability: "не застосовується, бо хвиля міняє те, що close ГОВОРИТЬ, а не те, чи він і інструмент узагалі запускаються"
  reliability.recoverability: "не застосовується, бо хвиля нічого не пише на диск і нічого не лишає після себе — вивід живе в пам'яті рівно стільки, скільки триває друк звіту"
  security.confidentiality: "**найважчий розріз цієї хвилі, і зізнання мусить бути повне.** Вивід gate може нести ключі, шляхи, вміст `.env` і вивід gitleaks про ЗНАЙДЕНЕ; вивід fmt/lint-кроку — це власний код проєкту (зміряно: 636 рядків diff-у джерела). Суд друкує це в лог CI, часто публічний, і в поле `report` JSON-пакета, який harness зберігає. Три речі, які роблять картину чесною: клас УЖЕ існує (один рядок друкується сьогодні); обхід, який хвиля знімає, вже вивалює ті самі байти в той самий лог, тож на раннері сумарна експозиція радше падає; і маскування — погана ідея, бо маскувальник, слабший за gitleaks, дає ХИБНИЙ спокій. Рішення хвилі: не маскуємо нічого і кажемо про це вголос у слові суду"
  security.integrity: "названо разом із resistance, а не поодинці: хвиля нікуди не пише, крім власного звіту, і нічого нового не виконує — але байти, що йдуть у термінал і в JSON, чужі, і про них нижче"
  security.non-repudiation: "не застосовується, бо хвиля не міняє ні комітів, ні їхніх тем, ні звітів рецензій — вона міняє один рядок вироку на екрані"
  security.accountability: "не застосовується, бо хвиля не міняє ні авторства, ні того, хто за що відповідає — вона міняє повноту цитати"
  security.authenticity: "не застосовується, бо в хвилі нема ні тотожності, ні підпису, які треба було б перевіряти: вивід чужої команди не автентифікується нічим і не претендує на це"
  security.resistance: "названо межу: цитований текст іде з чужої команди і не має інтерпретуватись — ні як розмітка, ні як керівні послідовності. `visible()` уже знімає кольори; хвиля мусить сказати, що робить із рештою керівних байтів"
  maintainability.modularity: "не застосовується, бо зміна живе в одній функції (`run_command`) і меж модулів не рухає"
  maintainability.reusability: "названо межу, і перша редакція тут обіцяла більше, ніж хвиля везе: `keel gate` німий так само, але він жене `run_test` і повертає `Outcome` — ІНШИЙ тип, і `gate.rs` у цій хвилі не чіпається. Спільного носія тут не буде; gate лишається німим, і це записано, а не сховано"
  maintainability.analysability: "тримає, і це те, заради чого хвиля: сьогодні червоний прогін CI НЕ МОЖНА дослідити — жоден рядок не називає кроку, що впав. Автор issue витратив чотири прогони на вгадування"
  maintainability.modifiability: "не застосовується, бо ні форми картки, ні норми хвиля не міняє; клауза контракту `tool-close` про слово вироку міняється тим самим комітом, що й код, і файл стоїть в області"
  flexibility.adaptability: "не застосовується, бо `run_command` мовно-незалежний: він жене `sh -c` і не знає, чия це команда"
  flexibility.scalability: "не застосовується, бо ціна залежить від розміру ОДНОГО виводу, а не від кількості тестів чи проєктів — і цей розмір названо в capacity"
  flexibility.installability: "не застосовується, бо хвиля не чіпає ні дороги установки, ні того, що лежить у теці версії"
  flexibility.replaceability: "названо межу: хто читає звіт машинно, побачить нове. Оскільки структури в `close --json` майже нема, ламати нема чого — але додане поле мусить бути додане, а не переозначене (`ENVELOPE` міняється, коли поле міняє сенс, і ніколи — коли додається)"
  safety.operational-constraints: "названо межу: у логах CI є ліміт на розмір, і звіт, що вивалює мегабайт, зробить прогін нечитним інакше. Вікно head/tail — вимога, а не оптимізація"
  safety.risk-identification: "названо ризик, якого перша редакція не бачила: `run_command` кличеться з ДВОХ місць — `close.rs:352` (verify контракту) і `close.rs:440` (ci проєкту). У самому keel три verify, і всі три — `cargo test`. Після хвилі червоний verify вивалить у звіт увесь вивід cargo test. Радіус ширший за gate, і вікно мусить це витримати"
  safety.fail-safe: "названо напрямок: якщо слова здобути не вдалось, суд каже це вголос — а не вдає, що падіння безсловесне. Мовчання, схоже на справне, гірше за коротку відмову"
  safety.hazard-warning: "не застосовується, бо хвиля не заводить нової небезпеки, про яку треба попереджати: вона робить наявну відмову читною"
  safety.safe-integration: "не застосовується, бо хвиля не додає ні процесу, ні нової межі — той самий `sh -c`, той самий захоплений вивід"
---

## Why

**Знайдено не нами.** Issue #45, відкрита з живого проєкту на keel
1.3.0 (`adapter = "ruby"`, `ci = "bin/ci"`), зустрінуто двічі за п'ять
днів. Коли gate проєкту падає, `keel close` друкує один рядок:

```
ci "bin/ci" — ВПАВ (Done in 116ms) — власний gate проєкту червоний,
хвиля не зливається (§7.16); повтори команду сам, щоб побачити її
слово повністю
```

`bin/ci` там — десять кроків: setup, rubocop, erb_lint, reek, flay,
flog, brakeman, gitleaks, тести, сіди. **Який саме впав — у логу
нема.** Червоний прогін CI дослідити неможливо.

Дужки брешуть: `(Done in 116ms)` виглядає як тривалість, а це останній
рядок чужого виводу — тут tailwind-ів. І порада «повтори команду сам»
на раннері адресована нікому.

## Вивід уже в руках

`close.rs:688` бере `child.output()` — увесь stdout і stderr. Далі:

```rust
Err(stderr.lines().rev().map(visible).find(|l| !l.is_empty())
    .or_else(|| stdout.lines().rev().map(visible).find(|l| !l.is_empty()))
    .unwrap_or_else(|| t("close-verify-no-words")))
```

Лишається **один** останній непорожній рядок. Автор issue має рацію
дослівно: воно вже захоплене, інакше не було б чого відлунювати.

## Чому це половина issue, а не вся

Перша редакція цієї картки везла й другу половину — німу батарею — і
**перевернула ціну**. Свіже око зміряло і спростувало:

| що казала перша редакція | що зміряно |
|---|---|
| «тип читають одинадцять місць» | grep дає 11 РЯДКІВ, з них 10 — це п'ять рук, лічених двічі (підпис і власна змінна), а 11-й — інший тип. Ззовні читає **один** файл: `close.rs:213` |
| «кожна рука вже ТРИМАЄ слово, його не пропускають» | свідомо відрізає слово **лише python**. У cargo, mix, minitest, rspec і node його **ніхто не розбирає** — треба **шість** нових читачів чужого виводу |
| «контракт `tool-adapter.md`» | **такого файлу нема.** Підпис `run_all` несуть дослівно ПʼЯТЬ `tool-adapter-*.md` плюс `tool-close.md` |

Останнє робило план невиконанним: не чіпати контракти — п'ять
знахідок суду форми §7.6 і `keel close` з exit 1; чіпати — дрейф §4.6,
бо їх нема в `files`.

Тож батарея їде **окремою хвилею**. Вона вчетверо дорожча за цю
половину і нікого не тримає, а ця половина знімає найгіршу частину
issue сьогодні й майже задарма.

## Що хвиля мусить вирішити, а не виявити в роботі

- **Вікно — на команду, не на звіт.** Інакше десятий verify з'їдає
  бюджет першого. Плюс стеля на весь звіт: `--json` кладе його в
  ОДИН рядок поля `report`.
- **Радіус ширший за gate.** `run_command` кличеться з двох місць —
  `close.rs:352` (verify контракту) і `close.rs:440` (ci проєкту). У
  самому keel три verify, і всі три — `cargo test`. Червоний verify
  після хвилі вивалить увесь його вивід.
- **Секрети: не маскуємо, і кажемо про це вголос.** Маскувальник,
  слабший за gitleaks, дає хибний спокій — це гірше за чесне
  «нічого не маскуємо». Деталі в `security.confidentiality`.
- **Слово в JSON — типізоване поле чи сама проза?** Якщо лише проза,
  harness мусить парсити текст — рівно те, від чого json робили.

**Чого хвиля НЕ обіцяє.** `keel gate` лишається німим: він жене
`run_test` і повертає `Outcome`, інший тип, і `gate.rs` тут не
чіпається. Перша редакція обіцяла «один носій для close і gate» — це
була обіцянка на чужу хвилю.

І порада «повтори команду сам» не стає розумнішою — вона стає
**зайвою**. Інструмент не вміє відрізнити раннер від ноутбука
(`env::var` у всьому `tool/src` — це PATH, KEEL_BRANCH, KEEL_HOME,
RUBYOPT, і все), і вчити його цього хвиля не буде: коли звіт
самодостатній, порада зайва скрізь.

## scenario: a-red-gate-carries-the-words-that-made-it-red

Червоний gate несе слова, що зробили його червоним.

Червоним народжується на пісочниці з багатокроковим `ci`, де падає НЕ
останній крок: сьогодні у звіті нема жодного слова про те, який крок
упав, — є останній рядок чужого виводу.

**Другий бік, обов'язковий:** зелений gate мовчить, як мовчав. Успіх
лишається тишею (§7.16), і хвиля не має перетворити зелений прогін на
стіну тексту.

**Третій бік:** вікно САМО каже, що воно вікно — чий вивід, скільки
рядків показано, скільки зрізано.

## transform: the-gate-hands-over-its-output

`run_command` віддає вивід із вікном і з рамкою, а не останній рядок.
Дужки після «ВПАВ» зникають: у них стояла цитата, і вона читалась як
тривалість. Порада «повтори сам» іде разом із ними.

Клауза `tool-close.md` про слово вироку міняється тим самим комітом.
