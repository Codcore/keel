---
scenarios:
  a-red-gate-carries-the-words-that-made-it-red:
    covers:
      - functional.correctness
      - reliability.faultlessness
      - maintainability.testability
      - interaction.user-error-protection
      - interaction.user-assistance

transforms:
  the-gate-hands-over-its-output:
    implements: [a-red-gate-carries-the-words-that-made-it-red]
    files:
      - tool/src/close.rs
      - tool/i18n/uk.ftl
      - tool/i18n/en.ftl
      - keel/contracts/tool-close.md
      - tool/src/main.rs
      - keel/contracts/tool-json.md
      - tool/tests/red_carries_words_test.rs
      - tool/tests/dead_assert_test.rs
decisions:
  functional.completeness: "хвиля повна тим, що бере ОДНУ половину issue #45 — вивід gate — і бере її цілком: `run_command` кличеться з двох місць, тож міняється вирок і `ci` проєкту, і `verify` контрактів. Друга половина issue (німа батарея) винесена в окрему хвилю, бо зміряно, що вона більша за цю половину і зачіпає п'ять контрактів із підписом"
  functional.appropriateness: "віддаємо те, що вже маємо. `close.rs` бере `child.output()` — увесь stdout і stderr у пам'яті, — і лишав один останній непорожній рядок. Це не «здобути слово», це перестати його викидати"
  performance.time-behaviour: "названо ціну, і перша редакція тут була неправдива. Рецензія 0070 R-8 зміряла на мільйоні рядків: перший крій матеріалізував `Vec<String>` з УСІХ рядків і жене `quoted()` по кожному — 5,6× інструкцій і 2,3× пам'яті заради шести кілобайтів друку. Виправлено: через `quoted()` ідуть лише ті рядки, які справді друкуються; решта тільки лічиться. Казати «коштує стільки, скільки коштує друк» можна лише про такий крій"
  performance.capacity: "названо три стелі, і всі три в коді. На КОМАНДУ — сорок рядків із кожного кінця, бо verify-ів у проєкті три плюс ci, і спільний бюджет дав би останньому зʼїсти те, що потрібне першому. На РЯДОК — чотириста знаків: вікно, що рахує рядки і не рахує довжини, пропускає цілим і трьохмегабайтний рядок, і прогрес-бар, чиї кадри розділені `\r` (зміряно: 289 665 байтів). На ВЕСЬ ЗВІТ — чотириста цитованих рядків, ЦІЛЛЮ: підлога в шість рядків на команду може її перевищити, і `flexibility.scalability` каже, чому це правильно"
  performance.resource-utilisation: "названо межу: ні диска, ні процесів хвиля не заводить, але ПАМ'ЯТЬ вона тримає — вивід команди вже лежить у `child.output()`, і вікно тепер не подвоює його копією всіх рядків. Перша редакція казала «не застосовується» і не бачила саме цього"
  compatibility.co-existence: "названо ціну для того, хто пінує keel: звіт `keel close` стане помітно більшим, і хто читає його очима або грепом, побачить нові рядки. Заслуги за зняття обходу ця половина НЕ бере: issue #45 каже, що для gate обхід ще тільки збираються будувати («we are about to do the same»), а наявний обхід — батарейний, і знімає його хвиля 0071"
  compatibility.interoperability: "рішення ухвалено, а не лишено питанням: слово падіння йде в ТИПІЗОВАНЕ поле JSON, не лише в прозу `report`. Причина: інакше harness мусить парсити прозу — рівно те, від чого json і робили. Ціна названа: у роботі міняються `main.rs:472` (там для close складається `fields` — сьогодні сам `blockers`) і `keel/contracts/tool-json.md:37`, який дослівно каже «close — blockers». Обидва в області"
  interaction.appropriateness-recognisability: "тримає: сьогодні `ВПАВ ({ $words })` читається як тривалість — у полі це дало `(Done in 116ms)`, і читач розумно вирішив, що gate ледве встиг побігти. Дужки після «ВПАВ» мусять піти"
  interaction.learnability: "тримає: рамка — нова річ у звіті, і вона каже про себе все, що читач мусить знати — чий потік, скільки рядків показано, скільки зрізано, і що нічого не маскується. Про кодування вона каже лише тоді, коли є що сказати: рядок «вивід не був цілком UTF-8» з'являється, коли декодер справді спіткнувся, і мовчить, коли текст цілий"
  interaction.operability: "тримає, але вужче, ніж писала перша редакція: після хвилі червоний gate можна дослідити з логу, не повторюючи прогону. Обхід, який чужий проєкт УЖЕ тримає, — батарейний, і знімає його 0071; тут знімається потреба будувати другий такий для `bin/ci`"
  interaction.user-engagement: "тримає, і число з першої редакції вже неправдиве: тоді один крок клав у звіт 636 рядків, тепер вікно тримає щонайбільше 82 на потік, а стеля звіту ділить чотириста порівну між усіма червоними. Вивід усе одно росте проти одного рядка, який був до хвилі, — але росте обмежено, і саме це читач мусить побачити"
  interaction.inclusivity: "названо межу точно. Цитата йде мовою і кодуванням команди; знімаються кольори і керівні байти, АЛЕ НЕ табуляція і не відступ — рецензія 0070 R-1 зміряла, чого коштувало інакше: каретка rubocop переставала показувати на порушення, а `col1\tcol2` зліплювалось. У діагностиці відступ І Є сенс. Якщо байти не були валідним UTF-8, суд каже це окремим рядком — і питає декодер, а не довжину, бо обрізана чотирибайтова послідовність довжини не міняє. Власні мітки звіту не доходять ні до сторінки, ні до пакета"
  interaction.self-descriptiveness: "тримає в чотирьох частинах, і четверту довелось лагодити після того, як стеля її зʼїла: рамка каже, ЧИЙ вивід, СКІЛЬКИ показано, СКІЛЬКИ зрізано — і коли зрізу не було, що показано весь вивід. Слово рамки живе під власною міткою, якої стеля не ріже: перший крій позначив його як цитату, і кожен блок утратив рядок «скільки зрізано» саме в тому звіті, де стеля його й зробила"
  reliability.fault-tolerance: "тримає в усіх трьох випадках, і третій довелось лагодити двічі. Порожній вивід дає `close-verify-no-words`; вивід із самих кольорів — те саме; вивід не в UTF-8 дає окремий рядок. Перша спроба питала ДОВЖИНУ (`stderr.len() != lossy.len()`) і була сліпа саме там, де підміна найтиповіша: обрізана чотирибайтова послідовність дає один U+FFFD на три байти, і довжина не міняється. Тепер питається декодер — `std::str::from_utf8(...).is_err()`"
  reliability.availability: "не застосовується, бо хвиля міняє те, що close ГОВОРИТЬ, а не те, чи він і інструмент узагалі запускаються"
  reliability.recoverability: "не застосовується, бо хвиля нічого не пише на диск і нічого не лишає після себе — вивід живе в пам'яті рівно стільки, скільки триває друк звіту"
  security.confidentiality: "**найважчий розріз цієї хвилі.** Вивід gate може нести ключі, шляхи, вміст `.env` і вивід gitleaks про ЗНАЙДЕНЕ; вивід fmt/lint-кроку — це власний код проєкту (зміряно: 636 рядків diff-у джерела в одного рецензента, 1843 в другого). Суд друкує це в лог CI, часто публічний, і в поле JSON-пакета, який harness зберігає. Чесно про напрямок: на раннері експозиція після цієї хвилі РОСТЕ — обходу для gate ще нема, будувати його тільки збирались. Клас при цьому не новий: один рядок чужого виводу друкується вже сьогодні. Рішення: не маскуємо нічого і кажемо про це вголос, бо маскувальник, слабший за gitleaks, дає хибний спокій"
  security.integrity: "названо разом із resistance, а не поодинці: хвиля нікуди не пише, крім власного звіту, і нічого нового не виконує — але байти, що йдуть у термінал і в JSON, чужі, і про них нижче"
  security.non-repudiation: "не застосовується, бо хвиля не міняє ні комітів, ні їхніх тем, ні звітів рецензій — вона міняє один рядок вироку на екрані"
  security.accountability: "не застосовується, бо хвиля не міняє ні авторства, ні того, хто за що відповідає — вона міняє повноту цитати"
  security.authenticity: "не застосовується, бо в хвилі нема ні тотожності, ні підпису, які треба було б перевіряти: вивід чужої команди не автентифікується нічим і не претендує на це"
  security.resistance: "названо, що саме робиться з чужим текстом: знімаються ANSI-послідовності і керівні байти, крім табуляції; більше нічого. Саме тому мітки, якими звіт розмічає власні цитати, — керівні байти: підробити їх із чужого виводу неможливо, бо `quoted` їх не пропускає. Знімаються вони і зі сторінки, і з поля JSON. Названо межу, якої закрити не можна: команда може надрукувати рядки, схожі на рамку, — текст лишається текстом, але видає себе за слово суду"
  maintainability.modularity: "названо межу, і перша редакція тут собі суперечила. Зміна НЕ живе в одній функції: хвиля додає публічний тип `RedCommand` в `exports` контракту, міняє публічний підпис `judge` і чіпає `main.rs` — це і є межа модуля, і сусідня відповідь `compatibility.interoperability` сама кладе `main.rs` та `tool-json.md` в область. Ціна прийнята: інакше слово падіння не дійшло б до JSON типізованим"
  maintainability.reusability: "названо межу і місце: вікно, рамка і стеля живуть у `close.rs` поруч із `run_command` і `quoted` — власним читанням, що прийшло на місце знятого `visible`. Хвиля 0071 їх перевикористає, і це записано в її картці як умова. `keel gate` лишається німим: він жене `run_test` і повертає `Outcome`, інший тип, і `gate.rs` тут не чіпається"
  maintainability.analysability: "тримає, і це те, заради чого хвиля: сьогодні червоний прогін CI НЕ МОЖНА дослідити — жоден рядок не називає кроку, що впав. Чотири прогони, витрачені на вгадування, issue #45 рахує за БАТАРЕЮ, не за gate: для gate ціна ще не заплачена, бо обхід тільки збирались будувати"
  maintainability.modifiability: "названо межу: клауза `tool-close.md` про слово вироку міняється тим самим комітом, що й код, і файл стоїть в області. Ні форми картки, ні норми хвиля не чіпає — але контракт чіпає, і сказати це треба прямо, а не через «не застосовується»"
  flexibility.adaptability: "не застосовується, бо `run_command` мовно-незалежний: він жене `sh -c` і не знає, чия це команда"
  flexibility.scalability: "названо ціну, межу і те, що межа мʼяка. Звіт не росте вільно: стеля — чотириста цитованих рядків, і ділиться вона ПОРІВНУ, бо бюджет «хто перший» лишив пʼятнадцять із двадцяти команд без жодного слова (зміряно другим колом рецензії). Але підлога в шість рядків на команду робить чотириста ЦІЛЛЮ, а не твердою межею: 101 червона команда тримає 606 рядків (зміряно третім колом). Це свідомий вибір — команда без жодного слова і є та вада, яку хвиля знімає. У JSON стелі на весь пакет нема теж свідомо: `red_commands` — поле для машини, кожен запис уже обмежений вікном, а машині потрібні всі команди, не перші"
  flexibility.installability: "не застосовується, бо хвиля не чіпає ні дороги установки, ні того, що лежить у теці версії"
  flexibility.replaceability: "названо межу: хто читає звіт машинно, побачить нове поле. `ENVELOPE` міняється, коли поле міняє СЕНС, і ніколи — коли додається; тут воно додається, тож конверт лишається тим самим"
  safety.operational-constraints: "названо межу: у логах CI є ліміт на розмір, і звіт, що вивалює мегабайт, зробить прогін нечитним інакше. Вікно head/tail — вимога, а не оптимізація"
  safety.risk-identification: "названо ризик, якого перша редакція не бачила: `run_command` кличеться з ДВОХ місць — verify контракту і ci проєкту. У самому keel три verify, і всі три — `cargo test`. Після хвилі червоний verify вивалить у звіт свій вивід так само, як gate, і вікно мусить це витримати; проба на червоний verify стоїть окремо"
  safety.fail-safe: "названо напрямок: якщо слова здобути не вдалось, суд каже це вголос — а не вдає, що падіння безсловесне. Мовчання, схоже на справне, гірше за коротку відмову"
  safety.hazard-warning: "тримає, і перша редакція тут суперечила власній `security.confidentiality`: та каже «не маскуємо і КАЖЕМО ПРО ЦЕ ВГОЛОС у слові суду» — а це і є попередження перед шкодою. Слово суду мусить сказати, що цитата чужа і не фільтрована"
  safety.safe-integration: "не застосовується, бо хвиля не додає ні процесу, ні нової межі — той самий `sh -c`, той самий захоплений вивід"
---

## Why

**Знайдено не нами.** Issue #45, відкрита з живого проєкту на keel
1.3.0 (`adapter = "ruby"`, `ci = "bin/ci"`), зустрінуто двічі за п'ять
днів. Коли gate проєкту падає, `keel close` друкує один рядок:

```
ci "bin/ci" — ВПАВ (Done in 116ms) — власний gate проєкту червоний,
хвиля не зливається (§7.16); повтори команду сам, щоб побачити її
слово повністю
```

`bin/ci` там — десять кроків: setup, rubocop, erb_lint, reek, flay,
flog, brakeman, gitleaks, тести, сіди. **Який саме впав — у логу
нема.** Червоний прогін CI дослідити неможливо.

Дужки брешуть: `(Done in 116ms)` виглядає як тривалість, а це останній
рядок чужого виводу — тут tailwind-ів. І порада «повтори команду сам»
на раннері адресована нікому.

## Вивід уже в руках

`run_command` бере `child.output()` — увесь stdout і stderr. Далі:

```rust
Err(stderr.lines().rev().map(visible).find(|l| !l.is_empty())
    .or_else(|| stdout.lines().rev().map(visible).find(|l| !l.is_empty()))
    .unwrap_or_else(|| t("close-verify-no-words")))
```

Лишається **один** останній непорожній рядок. Автор issue має рацію
дослівно: воно вже захоплене, інакше не було б чого відлунювати.

## Чому це половина issue, а не вся

Перша редакція цієї картки везла й другу половину — німу батарею — і
**перевернула ціну**. Свіже око зміряло і спростувало:

| що казала перша редакція | що зміряно |
|---|---|
| «тип читають одинадцять місць» | grep дає 11 РЯДКІВ, з них 10 — це п'ять рук, лічених двічі (підпис і власна змінна), а 11-й — інший тип. Ззовні читає **один** файл: `close.rs:213` |
| «кожна рука вже ТРИМАЄ слово, його не пропускають» | свідомо відрізає слово **лише python**. У cargo, mix, minitest, rspec і node його **ніхто не розбирає** — треба **шість** нових читачів чужого виводу |
| «контракт `tool-adapter.md`» | **такого файлу нема.** Підпис `run_all` несуть дослівно **п'ять** `tool-adapter-*.md`; `tool-close.md` несе саме ім'я в прозі |

Останнє робило план невиконанним: не чіпати контракти — п'ять
знахідок суду форми §7.6 і `keel close` з exit 1; чіпати — дрейф §4.6,
бо їх нема в `files`.

Тож батарея їде **окремою хвилею**. Вона більша за цю половину — 16
файлів проти семи, п'ять контрактів із підписом і проба, що без правки
не скомпілюється, — і нікого не тримає; а ця половина знімає найгіршу
частину issue сьогодні й майже задарма.

## Що хвиля мусить вирішити, а не виявити в роботі

- **Вікно — на команду, не на звіт.** Інакше десятий verify з'їдає
  бюджет першого. Плюс стеля на весь звіт: `--json` кладе його в
  ОДИН рядок поля `report`.
- **Радіус ширший за gate.** `run_command` кличеться з двох місць —
  двох місць — verify контракту і ci проєкту. У
  самому keel три verify, і всі три — `cargo test`. Червоний verify
  після хвилі вивалить увесь його вивід.
- **Секрети: не маскуємо, і кажемо про це вголос.** Маскувальник,
  слабший за gitleaks, дає хибний спокій — це гірше за чесне
  «нічого не маскуємо». Деталі в `security.confidentiality`.
- **Слово в JSON — вирішено: типізоване поле.** Проза `report`
  лишається, але harness не має парсити текст, щоб дізнатись причину —
  рівно те, від чого json і робили. Ціна: `main.rs` і
  `keel/contracts/tool-json.md` в області.

**Чого хвиля НЕ обіцяє.** `keel gate` лишається німим: він жене
`run_test` і повертає `Outcome`, інший тип, і `gate.rs` тут не
чіпається. Перша редакція обіцяла «один носій для close і gate» — це
була обіцянка на чужу хвилю.

І порада «повтори команду сам» не стає розумнішою — вона стає
**зайвою**. Інструмент не вміє відрізнити раннер від ноутбука
(`env::var` у всьому `tool/src` — це PATH, KEEL_BRANCH, KEEL_HOME,
HOME і RUBYOPT, і все), і вчити його цього хвиля не буде: коли звіт
самодостатній, порада зайва скрізь.

**Дрейф (§4.6), названий тут.** `tool/tests/dead_assert_test.rs` план
не називав. Він знадобився не випадково: власний суд проєкту «суд, що
не може впасти, — не суд» спіймав два мої НЕГАТИВНІ асерти, чиї фрази
не стоять ніде дослівно — `(tailwind: rebuilding)` (стара форма
вироку, з дужками) і `line-100` (середина виводу, яку вікно мусить
зрізати). Обидва живі й обидва впадуть на регресії, тож вони оголошені
серед `GUARDS` із причиною, як цей суд і вимагає. Це другий раз за
сесію, коли ця проба ловить автора.

## scenario: a-red-gate-carries-the-words-that-made-it-red

Червоний gate несе слова, що зробили його червоним.

Червоним народжується на пісочниці з багатокроковим `ci`, де падає НЕ
останній крок: сьогодні у звіті нема жодного слова про те, який крок
упав, — є останній рядок чужого виводу.

**Другий бік, обов'язковий:** зелений gate мовчить, як мовчав. Успіх
лишається тишею (§7.16), і хвиля не має перетворити зелений прогін на
стіну тексту.

**Третій бік:** вікно САМО каже, що воно вікно — чий вивід, скільки
рядків показано, скільки зрізано.

## transform: the-gate-hands-over-its-output

`run_command` віддає вивід із вікном і з рамкою, а не останній рядок.
Дужки після «ВПАВ» зникають: у них стояла цитата, і вона читалась як
тривалість. Порада «повтори сам» іде разом із ними.

Клауза `tool-close.md` про слово вироку міняється тим самим комітом.
