---
depends_on: [0019-ci-and-battery]

scenarios:
  ignore-reminded:
    proves: tool-init@015d65
    covers: [interaction.user-assistance, safety.hazard-warning]

transforms:
  ignore-row:
    implements:
      - ignore-reminded
    contracts: [tool-init@015d65, tool-adapter-cargo@68e86c, tool-config@5555a7]
    files:
      - tool/src/init.rs
      - tool/src/adapter.rs
      - tool/i18n/en.ftl
      - tool/i18n/uk.ftl
      - tool/tests/ignore_reminder_test.rs
  journal:
    chore: "bootstrap journal entries of the wave ride with it (V2-PROCESS)"
    files:
      - docs/uk/V2-PROCESS.md

decisions:
  functional.appropriateness: "свідомо без окремого тесту: третій подарунок поля — рама, що мовчала про теку збірки; доречність судить сам сценарій"
  functional.completeness: "свідомо: рама радить, ніколи не пише .gitignore — файл проєкту не її; повнота правил ignore — справа людини, і це сказано в Застереженні"
  functional.correctness: "тримає ignore-reminded: рядок читає справжній .gitignore і справжнє імʼя теки адаптера, ніколи не здогад"
  performance.time-behaviour: "не застосовується: одне читання файлу на біг init"
  performance.capacity: "свідомо без тесту: .gitignore читається цілим — файл правил, не дані; мільйонного рядка там не буває"
  performance.resource-utilisation: "не застосовується"
  compatibility.co-existence: "свідомо без нового тесту: наявний .gitignore не чіпається жодним байтом — рама лише читає (школа 0014: init нічого не топче)"
  compatibility.interoperability: "свідомо без нового тесту: правило рахується збіжним за рядком, як його читає git — teka/ або teka; вигаданих форматів рама не знає"
  interaction.appropriateness-recognisability: "свідомо без нового тесту: рядок стоїть серед інших рядків рами тією ж родиною слів (народжено/вже стоїть/натомість)"
  interaction.learnability: "свідомо без нового тесту: рядок каже точний текст, який треба дописати — рука людини знає, що робити"
  interaction.operability: "не застосовується: нових ручок нема"
  interaction.user-error-protection: "тримає ignore-reminded: сама помилка поля — git add . проковтнув теку збірки — і є тим, від чого рядок застерігає"
  interaction.user-engagement: "не застосовується"
  interaction.inclusivity: "свідомо без нового тесту: нові слова — ключами через i18n, доведеними в 0002"
  interaction.self-descriptiveness: "свідомо без нового тесту: рядок каже і що прочитано (.gitignore є чи нема), і чого бракує, і чиє це імʼя (адаптера)"
  reliability.faultlessness: "свідомо без окремого тесту: імʼя теки збірки — один дім в адаптері (BUILD_DIR), рама його питає, не переказує (школа 0015/0017)"
  reliability.fault-tolerance: "свідомо без нового тесту: нечитний .gitignore чи битий конфіг рами не валять — рядок каже, що не прочитано, і рама доноситься далі (школа 0014 R-1)"
  reliability.availability: "не застосовується: локальний бінарник"
  reliability.recoverability: "не застосовується: стану нема"
  security.confidentiality: "не застосовується"
  security.integrity: "свідомо без нового тесту: рама не пише .gitignore зовсім — цілість чужого файлу тримається тим, що його не чіпають"
  security.non-repudiation: "не застосовується"
  security.accountability: "не застосовується"
  security.authenticity: "не застосовується"
  security.resistance: "свідомо без фаззингу: вхід — текст файлу правил, читається рядками"
  maintainability.modularity: "свідомо без тесту: імʼя теки збірки знає адаптер (його дім), рішення «радити чи ні» — рама; жодна не лізе в чуже"
  maintainability.reusability: "свідомо без тесту: BUILD_DIR — експорт адаптера, ним скористається і наступний адаптер своєю хвилею"
  maintainability.analysability: "свідомо без нового тесту: контракти init і adapter-cargo кажуть, хто радить і хто знає імʼя"
  maintainability.modifiability: "свідомо без тесту: нова мова — свій адаптер зі своїм BUILD_DIR, рама не міняється"
  maintainability.testability: "свідомо без окремого тесту: пісочниці — справжні проєкти школи 0005–0019"
  flexibility.adaptability: "свідомо названо: рада існує лише під адаптером цього релізу; чужий чи невідомий адаптер — рядок каже, що імени теки нема кому назвати (школа 0017 R-3: названий-та-невідомий не зветься «не названий»)"
  flexibility.scalability: "не застосовується"
  flexibility.installability: "свідомо без тесту: рада — частина рами, іншого встановлення не потребує"
  flexibility.replaceability: "не застосовується"
  safety.operational-constraints: "не застосовується: жодних бігів, лише читання"
  safety.risk-identification: "не застосовується як окрема робота: загроза — тека збірки в git — і є сценарієм хвилі"
  safety.fail-safe: "свідомо без нового тесту: рядок ніколи не червонить раму — порада не є нестачею шматка (рахунок init не росте)"
  safety.safe-integration: "свідомо без тесту: рама росте одним рядком звіту; адаптер — однією константою; жодного наявного слова не переписано"
---

## Why

Щабель 18 — третій подарунок першого поля (FIELD-0001, «Подарунки
школі keel»). У полі `keel init` поставив раму мовчки, і перший же
`git add .` проковтнув теку збірки: `.gitignore` не було, а рама
про нього не сказала ні слова. Помилку виправили руками, але
інструмент, що ставить проєктові раму методики, мусить сказати про
теку, яку його ж адаптер створить першим бігом тестів — інакше
кожен новий проєкт наступає на ту саму граблю.

Рама **радить, а не пише**: `.gitignore` — файл проєкту, а не рама
методики (буква концепту перелічує, що ставить init: конфіг, скіли,
hook-конфіги, CI-файл, блок в AGENTS.md), і школа 0014 каже: рама
не топче жодного байта. Тож новий рядок звіту — порада з точним
текстом, який дописати, і ніколи не запис.

Відступи bootstrap, названі вголос: хвиля їде робочою гілкою сесії;
план їде план-гілкою §8.3 (експорт адаптера росте наперед коду);
журнал їде chore-трансформою.

## scenario: ignore-reminded

**Дано** проєкт із адаптером цього релізу і без `.gitignore`; той
самий із `.gitignore`, що теки збірки не згадує; той самий із
`.gitignore`, що її вже ігнорує; і проєкт, чий адаптер не названий
або невідомий,
**коли** біжить `keel init`,
**тоді** звіт несе рядок про правила ignore: без файлу — порада
завести його з іменем теки збірки адаптера; з файлом без правила —
порада дописати рівно цей рядок; із правилом — слово, що правила
стоять; без відомого адаптера — слово, що імени теки нема кому
назвати; у жодному разі `.gitignore` не створюється і не міняється
жодним байтом, і рядок не робить раму червоною (рахунок шматів, що
не стали, не росте).

## transform: ignore-row

Адаптер дістає дім імени своєї теки збірки — `BUILD_DIR`
(«target»), — бо це його знання (tool-adapter-cargo). Рама питає
конфіг про адаптер (`rust_adapter`) і читає `.gitignore` кореня:
рядки порівнюються обрізаними, правило вважається наявним, якщо
рядок — це імʼя теки з косою чи без (`target/` або `target`), як
його читає git. Чотири стани — чотири слова, і пʼяте для
нечитного файлу (рішення fault-tolerance шапки); звіт росте одним
рядком серед рядків рами, рахунок невдач не чіпається.

Застереження: рама нічого не пише — повнота правил ignore лишається
рукою людини (кеші, IDE, артефакти інших мов); правило шукається
рядком, не git-семантикою (заперечення `!target/` чи шаблон
`**/target` рама читає буквально — і мовчить радою, бо не вгадує);
чужий чи невідомий адаптер імени теки не має — своя мова прийде
своїм адаптером і своїм `BUILD_DIR`.

## transform: journal

Журнальні записи bootstrap їдуть своєю хвилею (школа 0009 R-1):
документ памʼяті лупа — docs/uk/V2-PROCESS.md (§9.10).
