---
depends_on:
  - 0068-the-closing-court-is-not-narrower-than-ci
  - 0071-a-red-battery-carries-its-words

scenarios:
  the-plan-has-its-own-reader-and-its-own-report:
    covers:
      - functional.completeness
      - interaction.user-error-protection
      - reliability.faultlessness
      - security.accountability
      - safety.fail-safe

transforms:
  the-court-asks-for-the-plans-reader:
    implements: [the-plan-has-its-own-reader-and-its-own-report]
    files:
      - tool/src/close.rs
      - tool/src/review.rs
      - tool/i18n/uk.ftl
      - tool/i18n/en.ftl
      - keel/contracts/tool-close.md
      - docs/uk/METHODOLOGY-V2.md
      - METHODOLOGY.md
      - docs/en/METHODOLOGY-V2.md
      - BACKLOG.md
      - tool/tests/the_plan_has_a_reader_test.rs
      - tool/tests/verdict_limits_test.rs
decisions:
  functional.correctness: "зміряно на 1.4.0, на пісочниці з повною хвилею і план-гілкою: `keel/reviews/` порожня, `keel check` exit 0, `keel close` exit 0. Тобто план-PR зелений і зливний без жодного рецензента — рівно те, що описує #53, і рівно в той момент, коли план ще можна відхилити як план"
  functional.appropriateness: "лагодимо ЗАСЛІН, а не пораду. Заявник назвав і те, чим лагодити НЕ можна: вимагати `keel/reviews/<хвиля>.md` на план-гілці. Такий файл переїде з планом на робочу гілку від народження і задовольнить ворота, які існують, щоб вимагати рецензію РОБОТИ. Тому ім'я інше: `keel/reviews/<хвиля>-plan.md`"
  performance.time-behaviour: "названо після ТРЕТЬОГО заміру, бо два перші були неправдиві (рецензії R3-2, R4-2). Зміряно накладкою на git, порівнянням дерева з мутантом, де заслін не кличе git зовсім: на план-гілці, де звіт Є і суд зелений, заслін додає **два** процеси git; де звіту нема і суд червоний — **три**. Дослівно: `branch --show-current` (`scope.rs` прямо каже, що його не запам'ятовують — кожен `plan_branch` новий процес), `show HEAD:./keel/reviews/<хвиля>-plan.md`, і на червоному шляху ще `rev-parse --verify --quiet HEAD`, яким `report_text` відрізняє «нема в історії» від «історії нема». Обидві перші редакції називали ціну меншою, бо рахували ВИДИ викликів, а не процеси; час міряють процесами"
  performance.capacity: "росте, і перша редакція казала протилежне. На план-гілці суд читає файл, якого раніше не читав: `report_text` робить `git show HEAD:./keel/reviews/<хвиля>-plan.md` і повертає його ЦІЛКОМ (це ж каже `security.confidentiality`). Приріст сталий — один файл на закриття, — і не масштабний: від розміру дерева не залежить"
  performance.resource-utilisation: "зміряно, і перша редакція казала «не застосовується» неправдиво. Диска хвиля не чіпає; ПРОЦЕСИ додає — на план-гілці заслін коштує два процеси git там, де звіт є, і три там, де нема (числа й дослівний перелік викликів — у `performance.time-behaviour`). Це та сама ціна, названа з іншого боку, а не її відсутність"
  compatibility.co-existence: "названо радіус, і він широкий: у КОЖНОГО, хто веде план-гілки, наступний план-PR почервоніє, доки в гілці нема `keel/reviews/<хвиля>-plan.md`. Це і є мета, але це зміна воріт для всіх. Твердження першої редакції — «два проєкти вже ведуть `*-plan.md` руками, їм хвиля нічого не ламає» — ЗНЯТО як незміряне (рецензія R-9): єдиний зразок у самій issue має ім'я `0014-plan.md`, а суд шукає `<повний слаг>-plan.md`, тож той файл вимоги не задовольнив би. Або імена там інші, ніж показує issue, або хвиля ламає саме той проєкт, про який казала, що не ламає. Перевірити це може лише той проєкт"
  compatibility.interoperability: "названо межу: ім'я `<хвиля>-plan.md` мусить бути таким, щоб суд закриття РОБОТИ його не зарахував. Зміряно на хвилях 0011 і 0013: `close` шукає `keel/reviews/<хвиля>.md` за точним іменем, тож суфікс `-plan` його не задовольняє. Перевірити це пробою обов'язково — на цьому тримається вся розв'язка"
  interaction.appropriateness-recognisability: "тримає: червоне на план-гілці мусить читатись як «плану бракує свіжого ока», а не як «щось не так із хвилею»"
  interaction.learnability: "тримає: заводимо нове ім'я файлу, і читач мусить знати, чим воно відрізняється від звіту роботи і чому їх два, а не один"
  interaction.operability: "тримає: порядок роботи міняється — на план-гілці з'являється крок, якого не було. `keel next` мусить вести до нього"
  interaction.user-engagement: "тримає з межею: на план-гілці у звіті з'являється рядок про брак рецензії плану; де вона є — тиша"
  interaction.inclusivity: "тримає: нові слова живуть в обох мовах"
  interaction.user-assistance: "головна половина другої трансформи: `keel next` сьогодні каже «злий план-PR» і «свіже око перед merge» В ОДНОМУ реченні, в такому порядку. Агент, що читає його згори вниз, мерджить. Порада мусить вести до рецензента ПЕРШИМ"
  interaction.self-descriptiveness: "тримає, і перша редакція тут була неправдива — рецензія R-3 зміряла. §9.9 у нормі знає лише `keel/reviews/<хвиля>.md`; після хвилі машина вимагає файлу, якого норма не називала. Тож текст НЕ лишається тим самим: §9.9 в обох мовах дістає речення про `-plan.md` і про те, чому імена різні. Інакше вийшло б те, чого ця хвиля й уникає — суд, якого нема в нормі"
  reliability.fault-tolerance: "названо напрямок, і перша редакція суперечила сама собі. Файлу нема — червоне; файл є і порожній — ТЕЖ червоне, окремим рядком, який каже саме це, а не «файлу нема» (зміряно: нуль байтів і самі пробіли дають вихід 1). Перша редакція писала, що порожній файл машина не читає, і тут же — «та сама межа, що для звіту роботи», хоч над звітом роботи порожній файл давно червоніє. Тепер обидва стани сказані нарізно і на обох стоїть проба: суд, який плутає «нема» з «порожній», шле людину робити не те"
  reliability.availability: "не застосовується: запуску інструмента хвиля не чіпає"
  reliability.recoverability: "не застосовується: хвиля нічого не пише на диск"
  security.confidentiality: "названо точно, і перша редакція тут була неправдива (рецензія R-6): хвиля читає ВМІСТ — `report_text` робить `git show HEAD:./…` і повертає файл цілком, а суд дивиться, чи він не порожній. Нічого з того вмісту у звіт не йде: у вироку стоїть лише ім'я файлу. Але сказати «читає наявність» було б неправдою про власний код"
  security.integrity: "не застосовується: ні запису, ні виконання"
  security.non-repudiation: "тримає: після хвилі в історії план-гілки лишається слід того, що план хтось читав. Сьогодні такого сліду може не бути взагалі"
  security.authenticity: "не застосовується: хто саме написав звіт, машина не засвідчує — і не вдає, що засвідчує"
  security.resistance: "названо найдешевший обхід, і перша редакція називала не той. Вона писала, що заслін обходиться ПОРОЖНІМ файлом; зміряно живим бінарником — не обходиться: нуль байтів дає вихід 1, самі пробіли й переводи рядка теж, і ловить це окремий ключ, який ця сама хвиля й завела. Найдешевший обхід — ОДИН непробільний символ: файл на один знак `x` дає вихід 0. Це свідомо і це та сама межа §7.8, що й для звіту роботи: машина тримає ФОРМУ — що файл є в історії гілки і що він не порожній, — а зміст тримає людина. Хто хоче обійти, обійде; хто забув — не забуде"
  maintainability.modularity: "тримає на рівні модуля і не тримає на рівні функції, і сказати треба точно. Обидва суди живуть у `close.rs` і ходять ОДНІЄЮ рукою `report_text` — їй передають різний аргумент. Але місця різні: заслін плану стоїть у `judge` із власним лічильником `plan_lacks`, а суд над звітом роботи — у `wave_state`, списком `lacks` усередині стану хвилі. Слід тієї ж роздвоєності: порожність перевіряється двома написаннями — `text.trim().is_empty()` для плану і `text.split_whitespace().next().is_none()` для роботи. Наслідок однаковий, третьої копії заводити не варто"
  maintainability.reusability: "тримає: два суди над двома іменами — одна рука з параметром, а не дві копії"
  maintainability.analysability: "тримає: звіт мусить казати, ЯКОГО саме звіту бракує — плану чи роботи, — бо два червоні під одним словом «рецензії нема» знову дадуть загадку"
  maintainability.modifiability: "міняє, і це заголовна теза власної трансформи, тож перша редакція («не застосовується») заперечувала саму хвилю. До неї нове джерело блокерів треба було вписати у ДВА місця — семичленний ланцюг `&&` у підвалі й вираз суми, — і чотири хвилі поспіль забували друге. Після неї — в одне: `held`. Межа названа тут-таки і стоїть у BACKLOG: сума править одним рукавом підвалу з трьох, і всередині того рукава лишився рукописний ланцюг із двох доданків"
  maintainability.testability: "тримає на чотирьох боках, і два з них додано аж на останніх колах. Перший: `close` на ШЕСТИ станах план-гілки — звіту нема; звіт є; звіт є, але лише на диску, не в історії; звіт порожній; хвиля скасована; слаг гілки не називає жодної хвилі. Другий: порядок речення в `keel next` обома мовами. Третій: СЛОВА пакета плану — що читання ЗАРАЗ і що файл зветься `keel/reviews/<хвиля>-plan.md`, названий у заголовку й у підвалі, обома мовами; без нього блокер кіл другого й третього був закритий самою правкою тексту. Четвертий: два стани («нема» і «порожній») сказані нарізно, і мутант, що міняє їх місцями, умирає"
  flexibility.adaptability: "не застосовується: від мови й адаптера хвиля не залежить"
  flexibility.scalability: "не застосовується: один шлях і один `git show` на суд, хоч би скільки було хвиль — заслін питає файл ТІЄЇ хвилі, чиє ім'я несе гілка"
  flexibility.installability: "не застосовується, бо хвиля не чіпає ні install.sh, ні launcher, ні теґів: вона міняє те, чого суд вимагає в уже встановленому інструменті"
  flexibility.replaceability: "названо борг, і він гострий: це ЗВУЖЕННЯ зеленого для всіх користувачів — план-PR, який учора зливався, завтра не зливається. Число ставить хвиля релізу; ця лишає борг названим"
  safety.operational-constraints: "названо після заміру, і перша редакція заперечувала час у тому ж реченні, де називала процеси. Місця хвиля не додає; часу додає — два-три процеси git на кожне закриття план-гілки. Числа в мілісекундах дає ця машина під цим навантаженням, і вони гуляють: три незалежні прогони по 30 повторів кожного виклику дали +26/+40, +35/+48 і +52/+77 мс (зелений шлях / червоний). Порядок — ДЕСЯТКИ мілісекунд, і саме це число варте картки; точнішого тут не буває, бо міряємо процеси операційної системи. Проти суду, який жене батарею тричі, операційної межі це не рухає"
  safety.risk-identification: "названо два ризики. Перший: хвиля червонить ворота в чужих проєктах — хтось зустріне це посеред роботи, і запис релізу мусить попередити. Другий, тонший: якщо суд шукатиме `-plan.md` НЕ точним іменем, а підрядком, він зарахує звіт роботи за звіт плану, і заслін зникне мовчки — рівно та вада, від якої застерігає заявник"
  safety.hazard-warning: "тримає, і обов'язок записано не лише тут: рядок у BACKLOG.md (рецензія R-9 зміряла, що самої картки мало — вона зникає з очей після merge). Попередження мусить стояти першим рядком запису релізу, бо звуження ширше, ніж «нові плани»: червоніють УЖЕ ВІДКРИТІ план-PR у мить оновлення інструмента, без жодної зміни в них"
  safety.safe-integration: "названо після заміру, і перша редакція тут була неправдива: хвиля ЗАПУСКАЄ нові процеси — той самий `git show` (і `branch --show-current`, і на червоному шляху `rev-parse`), яким суд питає, чи є звіт плану в історії гілки. Нового РОДУ інтеграції нема: це та сама рука git, якою суд уже ходить, і жодного нового зовнішнього засобу хвиля не заводить"
---

# 0075 — заслін стоїть на плані

Закриває issue #53.

## Що зміряно, і на чому

Бінарник **1.4.0 із релізу**, пісочниця: повна хвиля, її план на
гілці `plan/0019-a-full-wave`, тека `keel/reviews/` порожня.

```
$ keel check .   → exit 0
$ keel close .   → exit 0
$ ls keel/reviews/ | wc -l
0
```

План-PR зелений і зливний **без жодного рецензента** — у той єдиний
момент, коли план ще можна відхилити як план (§6.6).

І друга половина, та сама пісочниця:

```
$ keel next .
the step: this is the plan branch of wave 0019-a-full-wave (§8.3) —
prove the plan's fullness (keel check, the map), merge the plan PR;
the work will ride the branch "0019-a-full-wave" -- and then give the
plan a fresh eye: `keel review` here assembles the PLAN package and
names the answers worth rereading before the merge (§9.9)
```

«Злий план-PR», а тоді «дай свіже око **перед merge**» — в одному
реченні, в такому порядку. Агент, що читає згори вниз, мерджить.

## Чому це не дрібниця

§9.9 каже, що заслін тримає машина. Зміряно — не тримає. Те, що
машина справді тримає, — це рецензію **роботи**: `keel close` без
`keel/reviews/<хвиля>.md` роботу не закриває. Але затвердження **плану**
— це merge план-PR (§6.6), і до нього жодної роботи ще не видано. Той
merge агент може зробити сьогодні з зеленими воротами й без рецензента.

## Чим лагодити НЕ можна, і це слова заявника

Не можна вимагати `keel/reviews/<хвиля>.md` на план-гілці. Такий файл
переїде з планом на робочу гілку від народження і задовольнить ворота,
які існують, щоб вимагати рецензію **роботи**. Зміряно на хвилях 0011
і 0013: `close` шукає це ім'я за присутністю.

Тому ім'я інше: **`keel/reviews/<хвиля>-plan.md`**, і суд мусить
шукати його **точним іменем**. Пошук підрядком зарахував би звіт роботи
за звіт плану, і заслін зник би мовчки — рівно та вада, від якої
застерігає заявник.

## Один файл у scope, чия причина не в сценарії

`tool/tests/verdict_limits_test.rs` стоїть у `files:` через правку на
один рядок: `&whole_said[..600]` різав звіт по БАЙТАХ і панікував
усередині «я», ховаючи те, що асерт саме збирався сказати. Тепер
`chars().take(600)`. Це не сценарій цієї хвилі і не її тема — це
полагоджене мимохідь, і §4.4 вимагає оголосити те, чого торкнувся.

Зміна неспостережна, доки проба зелена: аргументи `assert!`
обчислюються лише тоді, коли асерт падає. Тобто в scope стоїть правка,
якої не тримає і не може тримати жодна проба батареї — і сказати це
краще тут, у картці, ніж лишити в повідомленні коміта, яке читають
рідше.

## Що лишилось свідомо, і це названо, а не змовчано

Коло третє назвало (R3-1), що сума блокерів править лише ОДНИМ рукавом
підвалу з трьох, а всередині цього рукава стоїть рукописний ланцюг
`held == 0 && verify_unrun == 0 && !ci_unrun`. Коло четверте побудувало
чотири стани (±звіт × ±довірені команди) і суперечності підвалу з кодом
виходу не знайшло: живої вади нема, вада структурна.

Лишаємо свідомо, і ось чому. Виправити це означає внести до суми ще два
доданки, які сьогодні не блокують нічого — тобто змінити, що таке
«блокер», у хвилі, яка про блокери не домовлялась. Рядок картки
«суму рахуємо ОДИН раз і вживаємо двічі» правдивий буквально про ту
суму, яку хвиля завела; ширшої обіцянки він не дає. Наступне джерело
«не бігло» доведеться дописувати руками в те саме місце — і саме тому
це не мовчання, а рядок у BACKLOG.

## scenario: the-plan-has-its-own-reader-and-its-own-report

На гілці `plan/<хвиля>` суд закриття червоніє, доки в історії гілки
нема `keel/reviews/<хвиля>-plan.md`. А `keel next` там веде до
рецензента **перед** merge, а не після.

Червоним народжується на пісочниці заміру: план-гілка повної хвилі,
`keel/reviews/` порожня, `keel close` сьогодні дає exit 0.

Другий бік: той самий план зі звітом плану — зелений.

Третій бік, і без нього заслін уявний: **звіт роботи не зараховується
за звіт плану**. Файл `keel/reviews/<хвиля>.md` на план-гілці лишає
суд червоним; і навпаки, `-plan.md` на робочій гілці не задовольняє
ворота §9.9 над роботою.

## transform: the-court-asks-for-the-plans-reader

`close.rs` питає точне ім'я тим самим способом, яким питає звіт
роботи (`report_text` з іншим аргументом). Слова в обох мовах, клауза
в `tool-close.md`, і речення в §9.9 обома мовами — бо машина не сміє
вимагати того, чого норма не називає (рецензія R-3).

Порядок у `keel next` кодом НЕ міняється, і це зміряно: українське
речення вже вело до свіжого ока перед merge, англійське — ні. Тобто
друга половина issue виявилась розходженням мов, і правиться в `.ftl`,
а не в `next.rs`. Перша редакція цього тіла писала інакше (R-7).

Два боки, без яких заслін уявний і які перша редакція проби не мала
(R-2, R-5): звіт ПЛАНУ не задовольняє ворота над РОБОТОЮ, і план-гілка,
названа іменем, якого в дереві нема, не мовчить.

І підвал: суму блокерів рахуємо ОДИН раз і вживаємо двічі — у підвалі
й у коді виходу. Ланцюг `&&`, який треба пам'ятати доповнити, забули
вже чотири хвилі поспіль (рецензії 0052 R-13, 0055 R-6, 0068 R-1 і
R-1 цієї).
