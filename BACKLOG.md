# Чого ще бракує

Черга щаблів після хвилі 0029. Кожен рядок тут — **підтверджена
вимірюванням вада або названа межа**, а не побажання: хто знайшов і
чим доведено — у стовпці «звідки». Порожній BACKLOG означає, що
драбина bootstrap скінчилась.

Цей файл живе в корені за §4.11 — разом із README це єдине, що
пишеться на main поза хвилею. Причина, чому він узагалі зʼявився:
черга півсесії існувала лише в чаті, а конституція (п. 8) вимагає,
щоб усе потрібне для продовження лежало в git. Свіжий агент читає
`keel next` і цей файл — і знає, що робити далі.

## Вади інструмента (підтверджені)

| що | звідки | чому болить |
|---|---|---|

## Межі, названі вголос і не зняті

| що | де названо |
|---|---|
| машина тримає кістяк перекладу, **не сенс** | хвилі 0028 і 0029; це межа за побудовою, а не борг |

**Знято хвилею 0032** — усе, що лишалось, крім однієї межі за
побудовою. Доручення рецензентові живе тепер у бінарнику: гігієна,
заборони (першими — та сама, якої бракувало рецензентові 0026),
контрфакти, чотири питання і форма звіту їдуть у кожному пакеті
`keel review`, і проба звіряє, що воно називає лише команди, які
інструмент справді має. Майстер питає `version`, `ci` і `trust`;
`keel setup` міняє відповіді, не чіпаючи того, чого не питав.
`keel concept` подає NEW-CONCEPT.md, і кожен текст каже, що взятий у
мить ЗБІРКИ. «Джерело правди» стало §1.8 в обох методиках — оператор
відповів «закінчуй усе в одній хвилі», і я прочитав це як «так».
Рядок про `keel close` знято як помилковий: власний target — рішення
§6.7, і хвиля 0031 назвала його ціну.

**Знято хвилею 0031** (вирок рецензента: «повернути на доробку»,
шістнадцять записів, тринадцять виправлено): вирок `keel check` несе
власні межі в самому підсумку — обрізана історія і скільки звірок
вона зʼїла, база порівняння та її свіжість, гілка, якої цей клон не
бачить у remote. Обрізаний і повний вирок більше не закінчуються
однаково; зміряно — 141 звірка проти нуля. Там же знято рядок про
`keel close`: власний `target` — **рішення §6.7, а не вада** (кеш із
чужого місця зсуває вироки), тож суд тепер каже ціну ПЕРЕД роботою і
відмовляється на вході, коли місця нема. Ціну зміряно: одне закриття
лишає 1,26 ГіБ, а не 4, як я був написав зі стелі.

**Знято хвилею 0030:** проби більше не лишають пісочниць. Зміряно
рецензентом: main лишав 200 тек / 398 МБ за біг, гілка — нуль, пʼять
бігів поспіль; пісочниця впалого тесту лишається навмисно і каже про
себе в stderr. Через це `keel close` знову можна ганяти, і рецензент
0030 його прогнав.

## Знайдено трьома аудитами і рецензією (2026-09-04)

Хвиля 0034 взяла чотири найважчі. Решта — **чотирнадцять** — тут
поіменно, кожна окрема робота:

| що | вага | звідки |
|---|---|---|
| `rev --write` друкує червоне, каже «нічого не дрейфує», виходить із **нулем** | середня | аудит багів B5 |
| `keel close` жене **червоний тест тричі** і мовчить про нього | середня | аудит багів B6 |
| `check` квадратичний за числом сценаріїв в одному файлі (32 000 → 89 с) | легка | аудит багів B16 |
| чи `keel close` закриває живого тезку тегом **знятого** — з боку `check` полагоджено, з боку `close` не зміряно | середня | рецензія 0035 |
| поведінка в справжньому GitHub Actions на події `pull_request` (detached HEAD грано лише локально) | легка | рецензія 0035 |
| «розріз» вживається в главі 10 десять разів і **ніде не визначений** | легка | рецензія 0033 R-13 |

**Знято хвилею 0036** — пʼять важких дір **у самій нормі**: код на
план-гілці тепер знахідка (§4.9); зниклий файл хвилі чи контракту —
знахідка, і `renamed_from` нарешті читається (§4.12); гілку `spike/*`
названо вголос, а `keel close` відмовляє її зливати (§4.13); вага
хвилі виводиться з файлу і називається, а повна хвиля на одній гілці —
знахідка (§6.8); хвиля з роботою і без жодного тегу — знахідка на
гілці, а не лише у `close` (§7.5).

**Знято хвилею 0035** — чотири речі, що вкусили б чужий проєкт у
перший тиждень: контракт із неіснуючим модулем більше не дає хибного
зеленого (суд одразу спіймав нашу власну фікстуру, що на ньому жила);
`keel --help` відповідає переліком команд, а невідомий прапорець і
зайвий шлях — відмова; два однойменні сценарії в різних хвилях —
знахідка (знятий сценарій імені не тримає); і гілку тепер можна
назвати через `KEEL_BRANCH` там, де git її ховає, — це вже стоїть і в
нашому workflow, і в тому, що пише `keel init`.

Доробка рецензії 0035 зняла ще й `{$snippet}` (рядок вище), і разом із
ним — цілий клас: повідомлення, що називає аргумент, тепер мусить його
діставати на кожному виклику, і це судиться на файлі слів проти коду.
Туди ж лягли: `--version`, `--help` у будь-якій команді, зайве слово
в `gate`/`plan`/`new`, `rev <тека> --write`, українська довідка,
`src/a/mod.rs`, глибокі модулі, імена, що виводять за межі crate,
однойменний контракт, і сторож проти дрейфу згенерованих файлів.

## Борг доручення рецензентові (не зроблено двічі)

Рецензент 0033 назвав шість речей, яких бракує в дорученні; у хвилі
0034 додано **нуль**, і самого боргу в цій черзі теж не було —
рецензент 0034 це зміряв. Разом із його пʼятьма:

| що дописати в доручення | звідки |
|---|---|
| **збери другий бінарник із бази (main) для контрасту** — половина важких знахідок 0034 зміряна саме так; без нього «регресія» і «давня вада» не розрізняються | рецензія 0034 |
| перезбирай після мутації файлу, вшитого `include_str!` — відкат файлу не відкочує бінарника | рецензія 0033 |
| повтори умови, в яких батарею жене **CI цього проєкту** | рецензія 0033 |
| перевір, чи кожен асерт **узагалі може впасти** | рецензія 0033 |
| **зіграй кожну межу, названу в тілі сценарію**, як окремий контрфакт | рецензія 0034 |
| **перевір, що жоден наявний суд не звузили** — назвати рецензентові diff проб окремо від diff коду | рецензія 0034 |
| перелічи окремим блоком усі зобовʼязання «**тоді …**» з тіл сценаріїв | рецензія 0034 |
| список чисел автора окремо | рецензія 0033 |
| скільки місця треба **мати** (два target-и ≈ 3,4 ГіБ плюс `keel close`) | рецензія 0034 |
| червона проба лишає пісочницю навмисне | рецензія 0033 |
| `keel/reviews/` як контекст | рецензії 0033 і 0034 |
| явний дозвіл комітити у власному клоні | рецензія 0033 |

## Аудит «обіцяно і не зроблено» (2026-09-05)

Оператор спитав: що ще забуто, як були забуті адаптери? Аудит пройшов
концепт, усі 93 параграфи норми, кожну команду і кожен контракт —
**двадцять знахідок, пʼять важких**. Кожна зміряна бігом, не читанням.

| що | вага | доказ |
|---|---|---|
| **дистрибуції все ще нема по-справжньому**: хвиля 0039 дала пінові руку (`KEEL_REF` в `install.sh`, і `version` називає команду), але `~/.keel/versions/` із кількома релізами поруч, launcher і **перевірений checksum** — не існують; тягнеться git-ref **за іменем**. І жоден опублікований теґ не несе теперішньої розкладки: `KEEL_REF=v0.8.9` відмовляє, бо v1 тримав крейт поза `tool/` — доки не буде теґу v2, працює лише коміт або гілка | важка | хвиля 0039 |
| **типізація кожного рядка вироку** в `--json`: сьогодні пакет несе те, що суд уже порахував (знахідки з файлом, межі, числа) і повну прозу полем `report`; речення вироку типовими полями не стали, і `structured` це каже вголос | середня | хвиля 0040 |
| **пакет `review --json` без структури**: пакет рецензента §9.9 несе три списки, і harness-ові вони потрібні — сьогодні там лише проза | середня | рецензія 0040 |
| **проза проти попереднього релізу** механікою не тримається: проба одного дерева не має другого бінарника, тож обіцянку «вивід не зрушив» тримає рецензент, а не суд | середня | рецензія 0040 R-1 |
| **команди, що пишуть, `--json` не мають** (`init`, `setup`, `plan`, `new`, `update`, `gate`, `hook`, `trust`, `concept`, `method`): harness, якому треба наслідок, питає `check` після них — межа названа, не закрита | легка | хвиля 0040 |
| `install.sh`, який тягне згенерований CI-крок, береться з рухомої гілки `main` — без піна і без перевірки: проєкт, приколотий до старого keel, виконує сьогоднішній скрипт | середня | рецензія 0039 R-13 |
| `check` друкує **статичний** «що саме перевірено» поруч із власними «не перевірено» в тому ж виводі; і рахує «2 речі не перевірено», назвавши чотири | середня | `check.rs:745`, `check.rs:732` |
| `-C <тека>` і `--branch <імʼя>` з концепту не існують; `--branch` живе як `KEEL_BRANCH`, і README про змінну мовчить | середня | `NEW-CONCEPT.md:333` |
| `keel hook <event>` концепту — «відповідач для agent-hook-ів (session start, перед записом)»; насправді `keel hook` ставить git-hook, аргумента нема, «перед записом» нема | середня | `NEW-CONCEPT.md:318` |
| `checksum` у `keel.toml` концепт зве «опційно», а парсер його **відмовляє** (`deny_unknown_fields`) | середня | `NEW-CONCEPT.md:166` |
| рішення оператора №5 і №7 не виконані: README не каже «v1 заморожено» і не вказує на гілку-довідку | середня | `NEW-CONCEPT.md:383,393` |
| «додати мову = покласти один перекладений файл» — неправда: третя мова коштує коду в трьох файлах і ~7 місцях (`LANGUAGES: [&str; 2]`, `include_str!`, чотири `match`) | середня | `NEW-CONCEPT.md:198` |
| кожен зелений `keel check` **у чужому проєкті** друкує внутрішню нотатку keel-а про власний щабель — і вже закриту хвилею 0035 | середня | `check.rs:757`, `uk.ftl:397` |
| **§4.13 бреше про власну механіку**: заборону spike тримає `close`, а норма каже «перевірка на PR червона». Проєкт, що поставить у CI лише `check`, заборони не матиме | середня | зміряно на `spike/*` |
| `keel close` — найважчий вирок — **не несе межі §7.8**: каже «кожен живий сценарій доведений» без застереження, що це значить «існує, збігається, проходить» | середня | `grep borders close.rs` — 0 |
| `check.rs` (59 КБ, найбільший модуль) **не має контракту**; так само `main`, `i18n`, `lib`, `refusal`. Суд §7.6 ходить лише в один бік | середня | 22 контракти, жоден не називає `keel::check` |
| **конституція п. 6 вимагає позначки «текстове» — її не несе жоден параграф**; 20 параграфів із 93 не називає ні проба, ні код, зокрема §4.11, §9.4, §10.5, §9.8 | середня | `grep` по §N.M у `tool/` |
| `keel version` не каже «які версії стоять локально» (обіцяно концептом; контракт це визнає, концепт — ні) | легка | `NEW-CONCEPT.md:321` |
| кореневий `PRINCIPLES.md` — принципи **v1**, англійського двійника v2 нема, на нього не посилається ніщо | легка | `grep PRINCIPLES` — 0 |
| концепт обіцяє позначку «чернетка» на незатверджених розділах — її не несе жоден розділ | легка | `NEW-CONCEPT.md:3` |
| два рядки цієї черги протухли: `init --no-hooks` уже слухається, `install.sh` уже полагоджено | легка | зміряно |

## Найбільша діра: адаптери інших мов

Концепт (рішення оператора, «Відкриті питання», п. 6) назвав стартовий
набір: **Elixir, Ruby, Python, TypeScript/JS**. Хвиля 0038 зробила
вибір адаптера і **Ruby**; лишається три мови.

Ціна третьої мови, зміряна рецензією 0038 (R-11), а не вгадана:
модуль, рядок у `Language::NAMES` — і дотик до **шести** місць, де
диспетчер розгалужується: `adapter` (тека тестів, один тест,
батарея, тека збірки, команда кроку), `holding` (розкладка модуля і
знаки коментаря), `ask` (перелік майстра), `next`, `generated` (крок
батареї в CI) і словник ×2 мови. Рядок «один модуль і рядок» стояв
тут і був неправдою.

| що | вага | звідки |
|---|---|---|
| адаптер **Elixir** (основний споживач — keel-agent) | важка | концепт |
| адаптер **Python** | важка | концепт |
| адаптер **TypeScript/JS** | важка | концепт |
| **RSpec** у ruby-адаптері: `spec/`, приклади за рядком, не за методом — друге читання і друга хвиля | середня | хвиля 0038 |
| **великий ruby-проєкт** (Rails, zeitwerk, gem із `Rakefile`) не міряв ні автор, ні рецензент — усі міри на пісочницях | середня | рецензія 0038 |
| ціна `run_all` у ruby: окремий процес на файл ×3 біги — на сотнях тестових файлів не міряно | легка | рецензія 0038 |

Поки адаптера нема, контракт можна тримати машиною через
`verify: <команда>` — цей шлях від мови не залежить (§7.16).

## Знайдено хвилею 0037

| що | вага | звідки |
|---|---|---|
| **суд проти зникнення тесту при заміні файлу**: я перезаписав `rev_write_test.rs` хвилі 0016 своїм, два тести зникли, і жоден суд не сказав — §7.15 дивиться на теги проти точки розгалуження, а тут тег зник і воскрес під іншим іменем в одному коміті | важка | хвиля 0037, спіймано ліком §5.6 |
| `keel.py` (312 КБ) і Python-`tests/` першої реалізації досі в корені — це **видалення**, тож окремою хвилею | середня | хвиля 0037 |
| хвиля 0016 має сценарій `stale-refs-rewritten` без тега в тестах — давній борг, який видно у `status` | середня | хвиля 0037 |
| відкат **злитої** хвилі і швидкий шлях для термінового фікса — §6.3-а покрив лише скасування початої | середня | рішення оператора |

**Рішення оператора 2026-09-04 (друге).** Хвилі й далі зливає в main
агент, а не людина кнопкою: §6.6 каже «затвердження — факт, що файл
дійшов до main», і цей факт створює merge, хай хто його робить.
Оператор про це знає і так вирішив; рецензент, що прочитає §6.6 і
побачить локальні merge-і, має знати, що це рішення, а не вада.
Серія хвиль виходить у main **одним PR**, який тисне людина.

## Питання до оператора (з рецензій і аудитів)

**Відповідь оператора 2026-09-04:** рецензент — **кожній хвилі**,
хай там яка вага. Зроблено хвилею 0037; §9.9 переписано в обох
текстах.

**Рішення оператора 2026-09-04 — обидва робить наступна хвиля:**

1. **§6.3 дістає названий виняток із доказом.** Суд над власною
   батареєю або інструментарієм **може народитись зеленим**, але тоді
   в коміті трансформи має бути записаний **мутант**: яку саме
   поломку внесено і як проба її назвала. Гарантія лишається —
   довести, що тест уміє падати, все одно треба, — а лазівка
   «перейменуй на chore» закривається: виняток названий, вузький і
   коштує роботи. Межу треба сказати вголос у самій нормі: **машина
   цього не перевіряє**, мутант живе текстом у коміті, тож тримає
   його чесність автора плюс око рецензента.
2. **Скасування початої хвилі** — і поки лише воно. Відкат злитої
   хвилі й швидкий шлях для термінового фікса чекають. Щодо фікса:
   частина того болю — не норма, а мій код (§6.8 каже «фікс на
   чотири рядки — легка хвиля», а `light()` цього не дозволяє
   жодного разу), тож полагодження коду може зняти біль само.

| що | звідки |
|---|---|

## Знайдено при оновленні README (2026-09-04)

| що | вага | звідки |
|---|---|---|
| **`keel.py` (312 КБ) і тека `keel/` першої реалізації** досі лежать у корені: два інструменти в одному репозиторії, і жоден суд не каже, який чинний | середня | оновлення README |
| кореневий `METHODOLOGY.md` — **копія** `docs/en/METHODOLOGY-V2.md`, і суду, що вона не розійшлася, нема (перед цим він був текстом v1, застарілим на пʼять місяців) | середня | оновлення README |
| v2 досі позначена «**чернетка**», а за нею живуть 36 хвиль, увесь інструмент і всі суди; текст каже «чинною лишається METHODOLOGY.md», якого в тому вигляді вже нема — **потрібне рішення оператора** зняти позначку (§8.6) | середня | оновлення README |

## Порядок

Спершу те, що коштує щодня і всім: `tests/common/` (диск), потім
`keel close` із чужим target, потім свіжість локального main і
обрізаний клон (обидва — про чесність суду), потім суд механіки PR.
Межі — за потребою, коли заболить.
