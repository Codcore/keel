# Вада: підказка радила `tool.tool.list(...)`

**Коли:** 24 серпня 2026, 00:40 UTC
**Де:** `KeelAgent.Session.Server.astray/2` — текст, доданий кількома годинами раніше
**Ціна:** Laguna — перша ж підказка була нісенітницею

Laguna покликала `tool.list` натівно і дістала:

```
`tool.list` is not called on its own here — it lives inside a program.
Put it in a fenced block tagged python:

```python
import tool
print(tool.tool.list(...))
```
```

## Чому

`bare_name/1` знімає розмітку й бере перше слово, але **не** префікс `tool.` —
його знімає окремо `ours?/1`. У новому тексті відмови я цього не зробив і
склеїв `tool.` з іменем, яке префікс уже мало.

## Ціна

Laguna крутилась на нативних викликах, і заслон спинив її за 10 ходів
(`going_in_circles`, 22 секунди) — це вже краще за 33 ходи, як було до заслону.
Але перша підказка, яку вона прочитала, радила неіснуючу форму.

## Лік

Префікс знімається там-таки, де будується порада.

## Перевірено

Тест подає натівний **`tool.list`** — саме те ім'я, на якому подвоєння й
виникає — і вимагає, щоб у пораді не було `tool.tool.`. Без ліку **червоніє**.

Перша редакція тесту подавала `list`, і на ній подвоєння не з'являлось: тест
був зелений в обох станах, тобто не перевіряв нічого. Ознака та сама, що вже
двічі траплялась за добу — **у перевірці немає того, що відрізняє випадок від
зразкового**.
